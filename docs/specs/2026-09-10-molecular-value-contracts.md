# 分子报告字段值合同与实施检查点

## 当前交付与验收

完整分子应用已由 [PR #88](https://github.com/skuyd/emr/pull/88) 按准确头 `625850ea9d54dd3309920346374b43d5f547f149`
完成独审及[精确CI](https://github.com/skuyd/emr/actions/runs/34544444229)，Squash为 `18181d9e68dd6adf0cf3438c63ac03fe9405b7f6`，随
[v1.19.0](../releases/v1.19.0.md)发布。63条及7项全局的每条功能结论和第8节均已由非作者据实际证据闭合，详见[最终验收](../verification/batches-one-five-acceptance.md)。
真实质量目标另列：病理1/10、癌种原评分、病灶retention=false和云未判定页保留；
治疗周期≥80%仍未建立，其规格/计划仍为implemented。真实M7及癌症第二次真实评估未启动，
生产仍BLOCKED。下文合同仍有效，带源码或时间的旧待交付措辞仅记录原检查点。

本规格细化[五批需求](2026-09-07-batches-one-five-requirements.md) B3-03 和
[第三批后续临床规格](2026-09-08-clinical-followup.md)的分子字段。
PR 70 已实际合入后的完整应用接入，见[分子应用合同](2026-09-10-molecular-application.md)；
下文 A0 的已完成范围及限制保留，不由后续开始开发改写为整体已交付。
本检查点称为 A0，其纯值合同范围交付状态为 `verified`；合同源码已随 [v1.15.0](../releases/v1.15.0.md)
包含发布，完整分子应用后续另行交付，A0原证据仍见[验证记录](../verification/molecular-value-contracts.md)。
它提供供解析、核对表单与选定输出共同使用的值校验模块；在A0检查点时分子功能整体尚未交付。
当前完整应用的独立交付见[分子应用验证](../verification/batch-three-molecular-application.md)。

## 实施顺序调整

2026-09-10 在五批自主开发授权内，将不依赖病理数据库的纯值合同提前实现。
分支 `feat/molecular-value-contracts` 从当日获取的 `origin/main`
`9675f0e3f61f96eb4895c364229b9da9d8a27bdb` 创建，使用独立 worktree。
此调整仅取代先前作者设计中“病理合并后才开始任何分子代码”的顺序限制。
病理 PR #70 中的共享字段、上下文、修订和输出接口仍须实际合入主线，才能开始依赖它们的接入。
本分支不复制尚未合并的病理实现；此前的设计与审查也不作为本次新代码通过的证据。

## Global Constraints

- 保留报告原文字、原单位、比较符、范围、约数、日期精度和各组件的未知状态。
- 类型校验不证明值来自原件，不证明标本或检测归属，不授予确认、导出或分享权限。
- 不根据数值推导 MSI/TMB 定性，不根据基因或药物推导诊断、推荐或治疗记录。
- 不修改旧 `clinical_schema.FIELDS`、旧模式版本、数据库、PD-L1 评分或应用路由。
- 原封存包、环境修订和独立审查证据保持原样；本次仅运行合成测试，不启动真实资料评估。
- 功能整体继续包含自动候选、来源核对、修订、搜索、选定导出及分享；纯值测试通过不替代这些验收。
- 遵守文档登记、Conventional Commits、必需 CI 与独审后 Squash 合并，以及 Release Please 自动版本规则。

## 值接口

生产模块为 `apps/facts/molecular_contracts.py`，公开 `validate_value(key, value)` 和
`display_value(key, value)`，字段描述只读。前者对未知字段、缺键、多键、类型错误及不一致
结构抛出 Django `ValidationError`，不改变传入数据；后者先校验，再保留报告原表达。
该显示结果是普通文字，后续 HTML 消费者仍按框架默认进行转义。

| 值族 | 必须保留或拒绝的行为 |
| --- | --- |
| 原文组件 | `PRINTED`、`NOT_PRINTED`、`UNKNOWN` 分开；已印必须有原文，其余不能包含猜测文字。未印需要后续原件范围证明，校验器本身不生成证明 |
| panel 与方法 | 名称、方法原词；规模保留整数文字、原计数对象/单位、比较符、范围、约数；不能解析的原断言保持未知数值，不能补零 |
| 检测日期 | 采样、收样、报告为三个独立字段；年、月、日和未知精度不互相填补，拒绝不存在的日期 |
| 变异身份 | 小变异、拷贝数、融合使用不同严格形状；保存原基因或位点、完整表达、已印编码/蛋白位点、带版本转录本、位置和体细胞/胚系/未知范围 |
| 融合伙伴 | 保留伙伴原顺序及各自转录本/断点；未印方向保持未知，不按字母排序，也不从连字符推断融合 |
| 分子数量 | 非负有限十进制字符串、`EQ/LT/LE/GT/GE/RANGE`、原单位状态、测量种类和约数；布尔、NaN、无穷、倒置范围拒绝；缺单位不能补百分号或转换比例 |
| MSI/TMB | MSI-L 不并为 MSS；类别、数量、TMB 定性各自保留，不互相推导，也不将参考阈值作为患者值 |
| 报告药物证据 | 原有序药名与明确组合关系、报告方向、等级及等级体系；未印体系保持未知，不跨体系排序，不从“未见耐药”推成获益 |

具体 JSON 键由模块公开合同及合成输入示例定义；来源窗口、断言作用域和真实父实体约束
仍须在后续应用合同中单独验证，不能只调用本模块就把一个字段标记为可用。

模式标识为 `MOLECULAR_VALUE_CONTRACT_V1`。单组件使用 `{state, raw}`，重复组件使用
`{state, values}`，保留顺序和重复项。数量使用 `{status, values, comparator, unit,
unit_state, approximate, measurement_kind, assertion, raw}`；panel 规模另含
`count_object` 原文组件。`UNRESOLVED` 必须是空数值列表和空比较符。
三个日期均使用 `{value, precision, raw}`；后续适配必须将 `raw` 映射到既有共享日期的
原文外层，不能用这一纯值输入替换已发布的日期持久化形状。

变异的 `COMPLETE/INCOMPLETE` 只描述声明的身份组件是否完整，不表示来源已核实或
临床结果可用。`display_value` 对缺失单组件返回空文字，调用者必须另行显示其保存的
`NOT_PRINTED/UNKNOWN` 状态。校验器不判断 `raw` 与枚举或数值的原件语义是否相符；
这种一致性、来源精度和患者结果作用域必须由后续提取、核对和持久化链路验证。

## 实施与验证计划

### Task 1: 建立可观察的合同测试

在 `tests/facts/test_molecular_contracts.py` 写入合成正反例，先运行并记录缺少实现时的失败。
覆盖原字符保留、缺失状态、完整身份差异、有序伙伴、日期精度、未解析计数、数值边界、
非法形状以及不变更输入；不能用校验器自己生成期望答案。

### Task 2: 实现共享校验与显示

仅在新生产模块实现本规格的值合同，使失败测试通过。使用当前主线依赖，
不修改既有影像值校验，不引入医疗知识服务或病理分支导入。
旧临床基础和影像数量测试作为兼容性回归。

### Task 3: 独立审查和交付

独立审查整个分支相对上述基线的变更及未跟踪新文件，检查遗漏字段、非法输入的异常类型、
原文保真和是否错误宣称应用完成。修复有证据的问题，再运行受影响测试。
运行 `python tools/verify_documentation.py`、Django 检查和相邻功能测试，登记实际结果。
本地证据齐备后提交 PR；必需 CI 未通过时保持待合并，不绕过检查。

## A0历史边界与后续完整应用

A0 不能覆盖原子回滚、实际上传持久化、迁移、租约、来源/修订变化后的输出失效。
A0设计时仍待后续实现的范围包括分子报告及检测分段、变异与阴性范围、MSI/TMB、分子报告中复用 IHC 的
PD-L1 适配、药物证据及完整消费者链路。旧真实质量限制和未判断资料范围继续保留。
