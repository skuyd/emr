# 癌种候选与指标排序实施计划

目标：完成[规格](../specs/2026-09-08-cancer-candidates-and-ordering.md)及五批 B3-04。
计划为 `active / implementing`，以下任务逐步实施；分支检查点不表示当前主线已交付。

2026-09-10 交付准备：`38d5447` 已完成叙述 A/B/C 应用及有界独审，原 Task 6
选定输出也已有有界独审通过；后续 `f0fee68` 修复两个输出 P2，完整源码独审及当前合成验收通过。
以下各阶段文字保留当时执行历史；当前测试、审查及未完成范围统一见[验证记录](../verification/cancer-ordering.md)。第二次真实评估
未启动；typed 病理适配已在实际主线病理接口合入后进入作者修订与合成验证，最终独审和 CI
尚未完成。本轮不改旧封存或真实产物，不把历史批准延伸到新修订。

2026-09-11 类型化作者修订：已在实际病理主线 `4b73d2e` 之上接入严格组织学字段、完整
标本来源、发布身份和原位置审核保留，并在 `e846263` 修正历史作者恢复、最后权限核验及
损坏报告关联的显示边界。当前源码的范围/合法身份/输出 12 项及 PostgreSQL/浏览器 16 项
通过；原 `cd66` 全仓和 `d4f3b7d` 模块检查保留各自源码归属。最终独审、后续实际主线格式
合流和精确 CI 仍待完成，不能以本段执行日志覆盖登记表交付状态。

新功能已在获取最新 `origin/main` 后，从实际 `92825c287e35f72bc3df0420eee72bb1e0d2f76a`
创建独立 `feat/cancer-ordering` 工作区；后续只合入实际主线。现有应用检查基线来自该源码
发布的 CI，不把规划当作新功能测试通过。后续文档/功能提交的确切身份分别记录。

2026-09-08，独立审查提出的上游未收集输入遗漏已补齐并通过增量复核。契约在
`e2024cff5ceb47a8f6739428ea3bea81d241d05e` 冻结；开始 Task 1，尚无癌种功能测试或发布结论。

Task 1 首个纯函数检查点已实现固定五字面、原字符串区间、断言/对象及稳定多重集排序。
最初 39 项测试在缺失行为接口上实际失败 30 项、通过 9 项；实现后 39 项通过。随后新增
逗号和否定作用域回归，40 项匹配测试实际失败 5 项、通过 35 项；修复后连同 6 项排序
测试共 46 项通过、零跳过。独立审查发现并列病名的共享否定和未支持的他人关系两项边界
问题；在 `538662ab034760e13f8c237fafdc2cca1214f66f` 修复后，63 项永久测试与原独立
10 项反例/控制共 73 项通过，零跳过，Task 1 有界复审通过。

Task 2 核心已实现不可变候选、完整收集结果、独立核对及显示选择。来源接口的 12 项真实
失败与持久化接口的 14 项失败/1 项权限控制均在实现后通过；新增撤销/来源恢复测试发现
的两处旧确认复活问题也已修复。当前永久核心共 96 项通过，另有 6 项真实 PostgreSQL
并发测试和 17 项迁移/审计检查通过，均零跳过。并发测试监控的两次失败来自锁队列关系
假设及事务内统计快照缓存，原结果保留；独立诊断清除统计快照后实际观察到三连接锁链。
历史迁移范围显式排除新应用后通过，新初始迁移逐行保留旧事实、检验、通知偏好及已有
导出/分享身份和快照，六个新表不自动生成候选或偏好。独立审查发现人工更正撤销确认后
仍借用原 OCR 置信度的问题，已在 `6d9a5ab7640334de97df2fb2ae056b4798f3ddc6` 修复，
原反例和实际提交反例通过复审。未更正原稿仍按自身来源条件判断自动排列资格。

Task 3 已在 `dfa5424c5183b5df9bb2f49898dc2fd4891282b2` 接入实际 worker。独立复审的
普通核心与 worker 共 57 项通过，另一次 PostgreSQL 共 8 项通过，均零跳过，范围有重叠。
覆盖 READY→PUBLISHED、重解析继承、成功零候选、真实 SQL 收集错误的事务隔离及 WRITE
恢复；READY 收集已提交后实际收回租约，旧 worker 不能发布，新 worker 可重建并发布。
Task 2 原问题关闭、Task 3 有界批准；不代表界面、指标调用点、选定输出或真实质量已交付。

Task 4 已实现核对与更正、候选/手动显示选择、历史、原件定位及家庭权限。HTTP 与审计
相邻检查共 35 项通过；另一次真实 PostgreSQL 界面并发 7 项及 Chromium 手机/桌面 2 项
通过，均零跳过。首次读取期间清理原件的两项真实失败已修复；旧页面遇到来源、作者或
权限变化后拒绝输出，失败表单沿用同一初始数据校验。浏览器首轮标签定位夹具错误已修正，
原结果保留；实际 360px 流程及截图检查覆盖错误关联、键盘提交、原件查看、历史和冲突。
界面在 `e66fbd6f6a6c84909ce1e676788b5135611eba8c` 获得有界独审通过：26 项 HTTP、
另一次 PostgreSQL/实际浏览器 11 项及追加手机双标签页 1 项均通过、零跳过。独立检查
包括中间修订作者清理、未知/历史表述更正，以及切换患者后的原标签页操作实际归属。

Task 5 已将顺序接入完整检验对比、趋势总览和选择器，显式联合图顺序不变。新增 8 项
及相邻 51 项普通检查通过、零跳过；真实 PostgreSQL 的 12 项提交并发检查通过。该次
还收集了 2 项旧浏览器控制并通过，2 项新浏览器因夹具错误预期未置顶指标的原有顺序而
失败；修正夹具后，新手机/桌面流程另跑 2 项通过、零跳过，旧失败证据保留。已目视手机
对比和联合趋势截图。既有字典缺少单位规则的指标仍保留在对比中，排序不增加趋势资格。
Task 5 的独立修复闭环如下；选定输出与真实原件质量仍属 Task 6—8，尚无发布结论。

## Global Constraints

- 默认 `AUTO`，符合最初需求的上传即组织；不得改成必须先选癌种。自动模式仅用当前、明确、
  可靠、无冲突的报告字面候选重排，不写系统诊断、不自动确认 Fact。用户显式选择覆盖自动。
- 初始配置为 `GENERAL`、`LUNG`（CEA/CYFRA21-1/NSE/ProGRP）与 `PANCREAS`（CA19-9/CEA），
  使用规格列出的既有标准码。固定字面映射，不引入新的医学映射、阈值或单位规则。
- 未核对候选所用 OCR 片段置信度全部须至少 `0.95` 才可参与自动模式；测试 `0.9499`、
  缺失及跨块低置信度。确认仍不能抹去来源失效或原文断言。
- 候选原稿、原 Fact/OCR、修订事件不可覆盖。显示状态独立于通知偏好，完整保留已有迁移数据。
- AUTO 完整性和输出指纹包含全部当前上游输入及收集状态；未物化候选、零候选、失败或规则
  过期也有明确表示。发布生命周期与不可变收集输入分开，正常激活不使覆盖永久失效。
- 稳定重排发生在现有过滤/分组/计算及输出选择交集之后。完整多重集和显式联合趋势顺序不变。
- 沿用实际请求作者、事务中权限复查、患者优先锁与全部来源/生命周期指纹；不得逆序锁定。
- 未合病理接口不作为起点；同类 typed adapter 只能在其实际进入主线后接入并单独验证。
- 所有永久文档位于 `docs/` 并同步登记/入口。私有原件、凭据及执行证据不进入远程。
- 每个完整切片完成必要测试及独审后，按已有授权建立中文 Conventional PR，精确四项 CI
  全绿后 Squash；Release Please 自动发布。不得手改版本字段，生产门禁独立。

### Task 1: 固定字面候选和稳定排序纯契约

新增 `apps/cancer_ordering/profiles.py`、`schema.py`、`matching.py` 和
`tests/cancer_ordering/test_profiles.py`、`test_matching.py`。读取现有
`apps/labs/dictionaries/phase-two.json` 验证标准码，不修改字典。

实现规格固定的五个完整字面，并固定诊断标题/分句、断言/对象、来源区间、未知理由和
配置版本；肺叶例子仅“右肺上叶浸润性腺癌”，不组合生成新别名。反例覆盖转移部位、
他人病史、疑似/否定、混合诊断、较长未支持病名内的字面子串、
仅器官或标记物、分期数字、跨分句/跨页、NFKC 原索引和低置信度。先记录生产匹配接口的
真实失败，再实现。不能用候选标签相同替代原位置一一对应。

排序函数接受已有条目和配置，返回稳定排列；独立用已知不同码/同码异质组与未知项验证
每个 ID 出现次数、输入未被修改和 GENERAL 顺序，不只检查几个置顶名称。

### Task 2: 来源、候选和选择的不可变持久化

新增 `models.py`、`sources.py`、`services.py`、`readmodels.py`、迁移及对应测试；参考
`apps/facts/models.py`、`readmodels.py`、`clinical_readmodels.py` 和
`apps/patients/access.py`。绑定患者必须通过真实 Fact.document，不假设解析版本必填。

实现按原位置幂等收集、候选修订、选择/撤销及有效 AUTO 解析。父来源和候选独立核对；
来源更正后捕获新身份，旧确认不可复活。保持一位患者多来源冲突、多个家庭实际作者及
完整匿名历史。持久化收集范围与结果状态，以完整当前上游 Fact/解析/修订/作者身份验证
覆盖，包括成功零候选。未收集、失败、部分覆盖和规则过期时 AUTO 回 GENERAL。
全部状态在服务端重建，不能信任提交的 token/上下文/作者。

以真实 ORM/API 操作验证跨患者、viewer 写入、陈旧修订、同文不同位置、候选集合新增、
来源排除/恢复、实际作者注销和重复撤销。历史迁移须保留旧 Fact、观察项、患者通知偏好、
已存在分享和导出，不在迁移中自动重跑真实原件。

### Task 3: 接入实际抽取与可恢复候选收集

新增 `apps.py`、应用初始化与 `extraction.py`，接 `config/settings/base.py`、
`apps/processing/pipeline.py` 的已存在事实持久化完成点。先核对该路径事务与锁顺序，
避免文档锁内反向请求患者锁。失败记录匿名错误并保留原件归档及已有事实，不导致资料丢失。
在当前处理租约保护下记录不可变 Fact/OCR 输入的完整收集结果；读时单独检查发布/active/
生命周期。实际跑通 READY→PUBLISHED 后自动模式，不能用临时 active=False 使成功覆盖
永久失效。保留失败与重试记录；写请求重新收集只生成新的范围结果，不覆写旧尝试。

主线 EXCERPT 适配器必须实际产出候选记录；既有资料通过明确的 WRITE 请求收集当前
事实，不在 GET 上写库、不改历史抽取。后续 typed adapter 接当前有效的
`specimen.histology`，同时验证标本/报告上下文和来源，禁止 IHC/变异推癌种。
持久化集成测试须从合成 OCR/真实数据库入口走完整路径，区分原始候选、人工更正与显示偏好。
明确验证新诊断 Fact 已发布但收集未执行、失败或只覆盖部分输入时的 GENERAL 回退，
以及零候选完成、规则过期、来源更正、WRITE 恢复收集后重新解析完整集合。

### Task 4: 患者界面、核对与当前状态

新增 `forms.py`、`views.py`、`urls.py`、`templates/cancer_ordering/` 和需要的样式；
接 `config/urls.py`、现有 `apps/patients/profile.py` 与患者资料界面入口。
复用原件查看和来源接口，不建立绕过资料权限的新下载路由。

实现默认 AUTO 依据、通用回退理由、候选冲突列表、核对/更正/排除/暂缓/撤销、显式候选或
手动偏好选择及恢复自动。先做实际 HTTP 红例，覆盖渲染期间来源/成员/作者变化后拒绝旧
内容。360px 页面、标签和错误关联、键盘操作、空状态与长原文均可使用。

### Task 5: 将排序接入完整对比和趋势

修改 `apps/labs/comparison.py::comparison_view`、`trends.py::trend_summaries` 与
指标选择器调用点；检查 `apps/documents/views/records.py`、`apps/labs/views.py` 及其模板。
保持 `joint_trend_views` 的显式用户码顺序；不得改变 `apps/documents/archive.py` 搜索条件。

用跨医院、同码不同单位/标本/方法、非数值、同日多条、未知码和过滤为空的真实 readmodel
验证 AUTO、GENERAL 及用户覆盖切换。逐 ID/多重集核对数据无损，比较点、基线和变化值
完全一致。动态冲突或失效立即回退通用，不用缓存的旧有效候选维持排序。

调用点独审发现日期表单再次提交会按置顶选择器重排显式联合图。修复后以独立隐藏序列
保留仍勾选指标的相对顺序，新勾选项稳定追加；隐藏序列不能重新选中取消的指标，且同样
校验当前可选码、去重及最多 8 项。31 项表单/趋势检查、4 项真实 PostgreSQL Chromium
流程通过（包含独立原失败探针与 GENERAL 控制，及手机/桌面的日期修改、删选和增选）。
独审原失败制品保留。修复提交 `a2ac69e2f4b904899fe60d3a4efb9578c4a04b05` 的定向独立
复验已通过：43 项普通检查、另一次 PostgreSQL Chromium 的 5 项流程均通过、零跳过；
包括独立空选择后逐次增删的手机流程。Task 5 有界批准，不据此标记整个功能完成。

### Task 6: 速查、导出和分享的选择与失效

新增 `exporting.py`/`output.py`；检查并接入 `apps/exports/content.py`、`services.py`、
`formats.py`、`forms.py`、`views.py` 和 `apps/patients/sharing.py`、`sharing_content.py`。
以当时实际已合主线格式为基线，保留全部既有数组/读取兼容，不复制未合分支实现。

先对已允许的指标应用顺序。候选/偏好正文需要独立明确选择；只选检验不携带诊断文字或
隐含源内容。私有依赖包含 AUTO 候选集合、冲突、当前来源和显示决定，不授予整文档权限。
AUTO 另外绑定全部当前相关上游 Fact/ParsingVersion、其修订/作者及收集状态，复用
Task 2 的完整性解析；未产生候选的新输入也必须使旧输出过期，不只指纹化显式输出文档。
为新可携带数据制定小型完整投影合同，检查 PDF 文字、JSON、CSV、ZIP 清单及 HTML 真正
生成的内容和拒绝路径，不仅比较内部字典。

在 `tests/integration/test_cancer_ordering_postgres.py` 用独立连接实际 COMMIT 检查
来源更正/新候选冲突/作者清理/撤权，以及新 Fact 发布但收集尚未发生或失败与预览、
worker 发布、分享及后续流块的竞争。实际打开并关闭生产
生成流，验证失效清理已经提交、旧快照拒绝且审计不重复。

Task 6 已实现独立的报告表述/当前显示偏好选择、最小来源引用、选定检验排序、PDF、
JSON、CSV、ZIP 与手机分享。未选择的新正文默认不携带；仅选择正文时无需包含原文档。
便携格式在本分支暂定为主线 1.4 之后的 1.5，集成前须按当时实际主线重新核对。

最初选择接口 7 项真实失败与 2 项会话夹具错误分别保留，修正会话夹具后与快照/任务
相邻检查共 27 项通过。格式/分享的 7 项真实失败修复后，相邻检查共 28 项通过；表单
入口的 6 项真实失败修复后，相邻检查共 27 项通过。随后普通功能与导出/分享相邻检查
共 325 项通过、零跳过。这些运行范围重叠，不相加作为独立覆盖数。

实际 PostgreSQL 的 16 项边界检查通过、零跳过，涵盖表单渲染后的来源/权限变化，
预览及分享渲染期间新输入、排除或作者清理，worker 发布前失效，以及实际 PDF/下载流
首次输出后的独立连接提交。已核对后续流拒绝、快照清理提交和分享审计不重复。
首轮实际 PostgreSQL Chromium 的选定 ZIP/手机分享/显示偏好更改后失效流程通过，
已目视手机分享和桌面预览。随后新增 4 项格式语义矛盾反例均实际失败；实现模式、
理由、显示配置与同包候选断言/对象的一致性校验后，47 项定向及相邻检查通过、零跳过。
最终当前源码的实际 PostgreSQL 复验共 17 项通过、零跳过：13 项 Chromium 流程包含
新正文及已有血糖、治疗、影像的真实导出/分享，另 4 项为影像并发与混合输出契约。
当前手机分享及桌面预览截图已再次目视核对。文档检查、Django 系统检查和迁移一致性
均通过；Task 6 独审尚未完成，不据此声称功能或真实质量已交付。

### Task 7: 独立审查、真实范围及浏览器验收

新增 `tests/browser/test_cancer_ordering_browser.py`，沿用主线已修复的浏览器事务夹具
（参考 `test_glucose_browser.py`）。运行真实上传/收集、自动置顶、冲突通用回退、手动覆盖、
切换恢复、原件查看、移动/键盘、实际输出和分享撤销流程。

每个核心检查点保存实际红/绿测试、源码身份及限制，取得有界独审。真实原件执行前另行
冻结范围、独立标注、gold/协议/评分器、源码和唯一运行参数；不把未知页作为阴性，不给
同一开发集冠以留出集，不因变更抽取器而重写既有评分或原始结果。

### 诊断叙述来源补充：检查点 A／B／C

2026-09-09，限定设计经非作者审查并采纳稳定发生身份 C1 后获准实施。以下为原癌种分支的
后续工作，状态仍为 implementing；不改五字面、原 Fact/OCR、金标、评分器、旧执行工具或
首次真实产物，不据此声称真实集已改善。各检查点先保留合成真实失败，冻结实现后分别独审。

稳定 occurrence 绑定原版本/原页/实际字面与原文位置，不包含可增删的父 Fact 集合、父确认/
修订/token/继承或当前历史作者；这些易变状态进入完整 input/live 指纹。若真正范围/规则变化
产生新来源世代，仍按稳定原位置关联旧审核障碍，明确需核对，既不继承确认也不恢复空历史
OCR 自动资格。必须覆盖 EXCLUDE、DEFER、CORRECT→REVOKE 后的父内容不变 CONFIRM、父新增/
删除/范围补充及作者变化；不同原位置和不同版本保持独立。

#### A：原位置与有限叙述纯契约

新增 `narrative_layout.py`、`narrative_matching.py` 和对应纯合成测试。先定位规格五类栏目，
保存逐字符原块映射及标题、语句、时间证明；不改 `facts.extraction.section_candidates`、
既有 reading_order 或 `SourceContext._excerpt_positions`。旧 `matching.py` 五字面及已审
行为保持，叙述适配器独立版本，不用虚构诊断标题。

先红覆盖分块标题/值、多行与同块重复字面、NFKC 原 offset、嵌套会诊/病史、同页两记录、
旁列字段、无标题/跨页/不连续范围、未知与零字面库存；每片段直接核对原块 Unicode 切片。
否定/疑似并列与明确新断言、家属/未知主体、历史/可靠日期与缺日期、主诉疾病字面与仅用药、
辅助检查陈述与仅数值、文献/质控/治疗适应证成对控制。标签高置信度但决定上下文 `.9499`
或缺失时不可 AUTO；旧纯匹配全部原控制仍成立。

#### B：双来源、真实父决定与上传 worker

增加 `NarrativeSource`、不可变实际父依赖、候选双来源 XOR 和迁移；更新 `sources.py`、
`services.py`、`readmodels.py`、`extraction.py`。稳定源/发生身份不受可变父集合污染。
同原文位置的实际父决定不可旁路；不生成虚假 Fact/ClinicalReport，不重写旧来源索引。

从合成真实 `run_processing` 证明无 Fact 页面仍可收集，正常 READY→PUBLISHED 后完整；重复
收集幂等，旧 Fact 原文/序号/修订/数量不变。数据库拒绝来源双空/双填或跨患者/页/版本；父
FK 清理保留捕获身份。显式 WRITE/真实 actor、旧确认失效、C1 全组反例、继承修订与历史作者
清理必测。完整库存涵盖新未物化叙述、零候选、失败/部分/旧规则；失败保存原件和匿名 FAILED。
真实租约失效不得发布，患者/文档锁顺序不倒置，迁移保留旧家庭/输出/分享状态而非跳过。

#### C：私有页面、输出兼容与实际提交

更新候选详情/标签与 `exporting.py` 明确分支。私有页显示实际栏目和当前父变化；无 Fact 时
只有已授权原页链接，不生成假事实路径。选定叙述只携带既有字段白名单和 `OMITTED_SOURCE`，
默认新正文仍空，旧 Fact 来源交集及全部旧数组/reader 保持，不增加原文分享权限或格式号。

实际 JSON/CSV/PDF/ZIP/分享 HTML 消费检查未选上下文/作者/私有 ID/指纹不外带。独立 PG 正常
COMMIT 覆盖渲染与无效 POST 初始材料、父更正/注销/撤权、新未收集输入、预览/worker/后续
流失效及清理提交，实际父确认与作者 FK 清理不得死锁。手机/桌面上传→核对→选择/恢复→
导出分享→父变更拒绝闭环后，冻结供非作者审查，不用合成例替代真实质量。

旧私有 mapper 强依赖 source_fact；第二次执行前须另派生版本，只读取已提交的新来源及片段，
不重跑来源构建/匹配或按金标定位。新 mapper 经合成/实际 CLI COMMIT 独审、完整身份与唯一
命令经根代理另行批准后才可运行第二次真实集；保留第一跑与全部未知、逐项 retained/lost/new。

### Task 8 执行检查与交付

PowerShell 使用实际工作区执行以下检查，并按变更范围补相邻测试：

```powershell
python tools/verify_documentation.py
python tools/verify_traceability.py
python tools/verify_release_gate.py
python manage.py check --settings=config.settings.test
python manage.py makemigrations --check --dry-run --settings=config.settings.test
python -m pytest -q tests/cancer_ordering
python -m pytest -q --ds=config.settings.postgres_test tests/integration/test_cancer_ordering_postgres.py
python tools/run_required_tests.py -q tests/browser/test_cancer_ordering_browser.py
```

PostgreSQL 使用已有专用测试配置和独立数据库，不把真实部署地址写入文档。必跑浏览器工具
的临时 XML 会自动清理；需要持久证据时额外保留真实结果或使用明确零跳过校验的 pytest XML，
不得声称不存在的制品。所有未执行、跳过及真实字段质量缺口单独记录。

在 `docs/verification/` 保存匿名功能/质量/交付证据，同步登记和入口。中文 PR 标题及实际
正文必须用 `tools/check_conventional_commit.py` 校验，检查暂存区和新提交全部父历史后
推送。确切 head 四项 CI 及独审通过后 Squash，核对 Release Please 实际标签/Release 后
补实际版本清单。五批整体验收仍由 Task 10 按全部 Bx-yy 完成。

### 2026-09-11 实际病灶主线合流

原癌症分支 `d0046c8188d6feaedab9ad668ec43dbf8a01df33` 后续正常合入已实际发布到主线的
病灶提交 `feca01075a05541d14fca5666e2c0a38e4a322ae`，不借用未合并功能。主线便携格式
已经是 1.6，本组合使用 1.7，完整保留病灶、云影像、IHC 及旧领域数组；旧 1.6 非空病灶
与云影像仍可读，缺少的癌症数组补空，旧版本非空癌症数组拒绝。前文暂定版本只代表历史
检查点，不能代替此次实际主线绑定；自动应用版本字段保持主线原样。

新混合回归检查真实 ZIP、两种选择表单、分享、三域依赖清理及旧格式边界，再以独立
PostgreSQL 和浏览器验证提交后的失效。原 d004 普通 1046 项通过；其 PG/浏览器组 149 项中
148 项通过，1 项仅为浏览器旧 1.5 格式预期与实际 1.6 不符，原结果保留，不能记为全绿。
本次修订更新正式组合预期至 1.7，仍待新合流独立审查和最终准确 PR 头 CI；真实第二次
评估仍为 `NOT_RUN`，不据此改变质量结论或生产门禁。
