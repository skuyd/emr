# 第三批稳定病灶、范围侧别与输出验证

本地应用实现、针对性回归和独立增量审查已完成；第三次真实执行及独立只读回读作为历史证据保留。
当前受测与受审应用提交为 `c0ba86f45af7cff3b0385042a73c9cd7b0a0a68c`，已整合实际云影像主线
`7b9fd7c`；后续 `87b6e4e` 正常合入实际发布主线 `c6a3ae3`，只增加自动发布版本和文档，
应用、测试、模板及 portable `1.6` 字节与 `c0ba86f` 完全一致。
本次仅重跑合成验证，仍待新增文档独审及 [PR #69](https://github.com/skuyd/emr/pull/69) 最终提交的 CI/合并。
**原真实保留门禁为 `RETENTION_REVIEW_REQUIRED`，没有改成通过；关联准确率未建立。**

对应[规格](../specs/2026-09-09-lesion-relations.md)、
[实施记录](../plans/2026-09-09-lesion-relations-implementation.md)及五批 B3-02。
[匿名验证制品](artifacts/batch-three-lesion-relations.json)保留真实计数、源码身份和证据
SHA256；原件、完整来源、数据库内容、截图及私有运行材料保留在本地，不是公开 CI 的依赖。
早期病理整合另见[匿名增量制品](artifacts/lesion-pathology-integration.json)；当前云主线合流见
[新匿名制品](artifacts/lesion-cloud-main-integration.json)，全部旧制品保持不变。
本页登记为 `verified` 仅指这些有界验证事实，不代表真实质量门禁、源码交付或 B3 全部完成。

## 1. 已实现的用户行为

| 能力 | 实现与回归入口 |
| --- | --- |
| 稳定身份和完整关系操作 | `apps/lesions/models.py`、`services.py`；`tests/lesions/test_relationships.py`、`test_proposal_decisions.py` |
| 双端原件、待核对提议与可见限制 | `apps/lesions/proposals.py`、`views.py`；`tests/lesions/test_proposals.py`、`test_views.py` |
| 尺寸/SUV/明确最大及可比性 | `apps/lesions/comparison.py`、`trends.py`；`tests/lesions/test_comparison.py`、`test_trends.py` |
| 来源、成员范围和历史失效 | `tests/lesions/test_source_lifecycle.py`、`test_side_scope_consumption.py`；`tests/facts/test_laterality_member_proof.py`、`test_source_read_freshness.py` |
| 搜索、速查和选定携带 | `apps/lesions/search.py`、`portable.py`；`tests/exports/test_lesion_selected_material.py`、`test_lesion_portable_formats.py`、`test_lesion_output_bindings.py` |
| 真正提交竞争与手机浏览器 | `tests/integration/test_lesion_output_freshness_postgres.py`、`test_lesion_read_freshness_postgres.py`、`test_lesion_cloud_output_postgres.py`；`tests/browser/test_scoped_laterality_output_browser.py` |

确认、改派、拆分、解除、命名及撤销保留作者与追加式历史。自动提议不等于确认。
成员侧别只限定被原文证明的成员，旧标量缺少范围证明时保留未知范围。来源、权限或历史
作者变化使页面和旧输出失效；选择关系不能携带未选报告或扩张分享范围。

## 2. 测试与源码归属

| 阶段 | 实际结果 | 归属及限制 |
| --- | --- | --- |
| 原核心检查点 `5c36d4b` | 100 项普通通过，0 跳过 | 关系/临床/影像和历史迁移回归；PG 与浏览器为该阶段另行保存的运行 |
| 范围修复独审 `cd82dd7` | 31 项普通、12 项 PG 通过，0 跳过 | 关闭成员自身来源和渲染后来源/权限两项 P2；不是当前头全量测试 |
| 主线集成 `edaa2bd` | 120 项普通、31 项 PG、SQLite/PG 各 2 项 Chromium 通过，0 跳过 | 实际运行在待合并状态，测试文件字节等于合并提交；保留原执行 HEAD |
| 输出修复 `33ec969` 作者验证 | 96 项普通、2 项原无分隔显示反例、2 项 PG 通过，0 跳过 | 仅四文件修复；原先失败不删除，2 项反例与其他运行有重叠 |
| 输出修复 `33ec969` 独立验证 | 55 项普通、2 项 PG 通过，0 跳过 | 检查省略标志、长度边界、严格模式、类型化范围及隐藏来源变化 |
| 病理与治疗主线整合 `89aa430` 作者验证 | 1399 项相关普通、48 项 PG、8 项实际 Chromium 通过，0 跳过 | 迁移双叶、病理上下文与范围侧别共同输出、最终权限复核及失效状态；源码执行前后不变 |
| 同一整合增量独立验证 | 37 项合成测试通过，0 跳过，0 剩余 P1/P2 | 含 7 项独立迁移和最终权限探针；不替代最终提交的完整 CI |
| 云影像主线合流 `c0ba86f` 定向验证 | 41 项通过、0 跳过 | portable `1.6`、非空旧 `1.5` 云表及病灶/IHC/云混合输出；合流检查点测试，先前两个临时导入别名错误保留 |
| 同头作者冻结回归 | 775 项相关普通、86 项真实 PG、22 项实际浏览器通过，均 0 跳过 | 三组各自核对全部 1373 个 tracked 文件前后字节不变；1399 项属于旧 `89aa430`，不是当前全套 |
| 同头独立合流审查 | 23 项现有 portable/IHC、6 项混合绑定及渲染后变化、1 项来源锁/清理/双表单检查通过，均 0 跳过 | 三次执行分开计数，审查增量 0 P1/P2；带探针失败的整次执行不标为全绿 |

这些运行不能累加为一次测试。浏览器实际使用合成原件，覆盖原页核对、成员表单、稳定
标识、ZIP 下载回读、手机分享和父字段撤销；测试者查看了实际截图。PG 使用独立连接、
不同 backend PID 和正常 COMMIT，验证变化发生后停止下载或丢弃分享正文。
历史 Chromium 结果仍属于 `edaa2bd` 的测试文件集合；旧 8 项属于 `89aa430`，
另行保存实际截图，已查看手机选定预览和限时分享中的标记、单位缺失与所选上下文限制。

当前合流保留血糖、病灶完整历史与云来源的私有文档锁和清理依赖，公开 `documents` 不因
来源选择而扩大。三类绑定任一丢失，预览和分享整体失效；在分享正文渲染后删除任一类
绑定，也返回 410 并清空旧快照。导出与分享表单同时保留病灶和云来源选项，JSON/CSV/PDF/ZIP
及分享保留两类选定语义。旧 `1.5` 非空云来源文件仍可读，但声明 `1.5` 的非空病灶图被拒绝。

独审第一次执行为 23 通过、4 个探针构造失败：云来源不属于显式选定字段的报告范围，被正确拒绝。
第二次 4 个探针因尚未建立实际登录会话被拒绝。第三次为 6 通过、1 个观察断言错误：
首次已取得完整来源联合锁，随后血糖子读取再次取其来源锁，探针错误地要求最后一次仍为全集。
修正后单独 1 项通过，并确认每次锁调用都在事务内。各版探针、XML、日志和初次清单收集器
对中文路径处理失败均保留；没有为这些探针失败修改应用或降低绑定、权限断言。

`87b6e4e` 的发布主线同步与本次文档修订仍待非作者增量审查；没有重跑上述完整矩阵，
也不将文件哈希不变本身当作新增测试通过。最终 PR69 精确 CI 仍须在实际交付头运行。

早期病理整合实际修复来源失效响应被改成 409 的问题：所有响应仍先完成最后权限核验，
来源已失效时保留 410，并清除旧正文。混合 IHC 与病灶实际 JSON、ZIP/CSV、PDF 和分享回读
保持所选语义及独立输出别名；0005 合流迁移保留两条已存在的迁移叶及历史数据。
原失败、被中断的宽范围测试及测试夹具修订全部保留；更新版本预期和文件哈希不计测试通过。

曾发现的整组范围、成员引用父区间、渲染后过期正文、省略后长度以及无分隔 URL 吞掉
限定显示等问题均保留原失败与各自修复审查。历史全量、测试夹具错误和检查脚本错误不
重标为当前源码的通过结果。这里只汇报能由实际 XML 和冻结源码对应的运行。

## 3. 第三次真实执行范围

执行先冻结原件清单、原 gold、原协议、mapper、应用与字典身份，再经独立审查批准。
只启动一次外层命令，包含两个隔离的新内存数据库阶段；两个子阶段均退出 0，没有重试。
外层因原正确目标保留差异退出 1。执行后的所有保护项检查通过。

| 阶段 | 实际范围 | 不能由此推断的内容 |
| --- | --- | --- |
| 关系和同库 FIELD | 8 来源、8 报告范围、2 个原件已核患者组；完整文档共 19 页，11 页在关系范围外 | 不是全 64 来源的关系评测，也不是全部 19 页关系覆盖 |
| EXCERPT | 64 来源、124 页、每来源独立患者容器 | 不建立或预测跨来源病灶关系 |

合计 72 次来源处理、64 个不同原件。第二阶段不是额外关系预测；第一阶段的 144 个 FIELD
从同一已执行数据库直接捕获，未再调用字段生成。字典为 `phase-two-1`。固定 OCR 被重放，
不声称本次重新测量 OCR 服务准确率。

独立回读核对 1259 个 tracked 文件、616 个生成源、8877 份旧制品、128 份原件/OCR、
19 份准备材料及 650 个实际产物；未导入应用、mapper 或评分器，未重新预测/评分。
两个阶段各自捕获的 102 个模型在只读捕获前后保持一致。144 个 FIELD 均为 PENDING、
revision 0；没有稳定病灶、用户关联决定或 FactRevision。一个 PROPOSE 修订只是提议创建。

## 4. 关系、字段与未知范围

关系原 gold 保留 36 节点和 630 对：80 对同报告、373 对解剖不兼容、128 对跨患者边界、
49 对未知。阳性临床关系为 0，precision/recall/F1 均为 `null`。
当前 33 个观察中 9 个映射、24 个未映射，27 个 gold 节点缺失；一个 PENDING 提议的端点
映射和来源理由未充分验证，不能计为正确临床关联。第一、第二次各自 7 个旧映射都保留，
两套旧目标不同，不能把它们当成同一集合。

| 固定 FIELD 任务 | 严格正确 | 错配 | 漏提 | 额外 | 原 present / absent | 值正确 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 七字段 | 44 | 43 | 18 | 10 | 105 / 32 | 75 |
| 影像量化 | 35 | 10 | 9 | 1 | 54 / 14 | 37 |

错配包括来源不充分，不能全部解释为医学内容错误。144 FIELD 声明的 322 个实际片段均与
固定 OCR 的页、reading_order、Unicode 截片和 polygon 一致；这不重新判定语义或提高分数。

## 5. 旧正确目标逐项保留与接受的取舍

“证据投影相同”只比较被冻结合同中的字段值、来源标志、页、reading_order、区间、片段
和几何；不是整个私有对象或数据库 UUID 字节相同。

| 原基线 | 严格正确保留 | 值正确保留 | 旧值正确目标中证据投影相同 |
| --- | ---: | ---: | ---: |
| 最初临床七字段 | 24/24 | 45/46 | 34/46 |
| 已发表临床七字段 | 36/37 | 72/73 | 53/73 |
| 已发表影像兼容七字段 | 36/37 | 72/73 | 53/73 |
| 最初量化 | 27/27 | 35/35 | 26/35 |
| 已发表量化 | 33/33 | 35/35 | 33/35 |
| 第二实际七字段 | 42/42 | 73/73 | 72/73 |
| 第二实际量化 | 35/35 | 37/37 | 37/37 |

三处正确性损失指向同一个旧整组侧别目标：原文组内包含未明确侧别的成员和明确双侧的
成员。本次旧标量评分仍为 MISSING，新类型只捕获一个 `NAMED_MEMBERS_ONLY` 字段，父部位
和成员自己的原区间均独立核对。该新字段未纳入旧标量 gold、未评分，不能补偿旧缺失。

按此前已接受的范围契约和[五批 B1-03](../specs/2026-09-07-batches-one-five-requirements.md)
对退化解释取舍的要求，接受这个表示边界，继续交付准备；不恢复含义过宽的整组标量，
不改 gold、评分器或原退出码，不运行第四次评测。原 `RETENTION_REVIEW_REQUIRED` 和
`retention_gate_passed=false` 保留，这个决定不是“原门禁通过”。

第二实际七字段虽保留全部正确性，仍有一个正确目标的原区间收窄，故证据投影为 72/73。
更早两次关系局部字段账本分别仅保留 44/81、76/78 个已记录值/片段/几何，且旧账本缺少
完整 reading_order，完整原块身份保留不能宣称通过，也未向旧资料回填来源。

## 6. 摘录保留及交付限制

64 来源的 202/202 个原 EXCERPT 完整 payload 多重集和来源状态保留，新增/删除/状态变化
均为 0；当前 raw 文件与旧预测及 native 即时副本逐字节相同。既定评分仍为 97 正确、
104 错配、31 漏提、1 额外，精确率 97/202、召回率 97/232，34 未审页和 2 个不可判断字段
保留。旧 payload 不含全部原块身份，完整 payload 相同不补足这项历史限制。

源码功能与合成回归已形成有界证据；真实关系零阳性、范围字段未独立评测、未映射观察及
字段来源差异继续保留。当前没有该功能的确切 PR 头 CI、合并或发布证据，版本关联为空。
生产仍按[发布门禁](release-gate.md)执行，不由此页改变。
