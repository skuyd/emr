# AC-02 through AC-07 verification record

Recorded at **2026-08-30T14:40:03Z** (UTC) from baseline commit
**73abdef** on Python **3.11.9**, Django **5.2.17**, and pytest **8.4.2**.
All files and identity values used by these tests are generated synthetic
fixtures. No medical sample content is included in this record or in the test
sources.

## Upload acceptance, isolation, and privacy — RUN

```text
python -m pytest tests/acceptance/test_ac02_ac07.py tests/security/test_upload_isolation.py tests/privacy/test_upload_logs.py -q
9 passed in 2.41s
```

This run verifies:

- AC-02: one batch of exactly 20 valid three-page PDFs is durably accepted as
  20 documents and 60 pages;
- AC-03: after a successful response, a new authenticated session can recover
  batch status and reopen the byte-identical original through a newly created
  local private-store adapter;
- AC-04: malformed upload bytes create no document, and the same upload item
  can be retried successfully;
- AC-05: a no-structured-result processing outcome becomes `仅原件` while the
  original remains byte-for-byte readable;
- AC-06: an exact same-patient duplicate creates no second document and opens
  the existing document and original;
- AC-07: an unrecognized date is shown as `日期未识别` without a required data
  entry field;
- upload, batch status, summary, and original endpoints all return indistinct
  404 responses to another account, and soft-deleted originals are unavailable;
- original responses use opaque filenames, same-origin and no-sniff controls,
  and `private, no-store` caching; missing private objects return a safe 503;
- captured upload, duplicate-cleanup, and untyped-pipeline-error logs contain
  none of the synthetic filename, phone-like text, patient label, OCR-like text,
  or exception medical-content marker.

The exact-boundary PDF fixtures are rendered and structurally inspected using
pypdf **6.16.2** and pypdfium2 **5.13.0**. Image checks use Pillow **12.3.0**
and pillow-heif **1.5.0**. File detection uses Magika **0.6.3**.

## Application and complete Python regression — RUN

```text
python manage.py check
System check identified no issues (0 silenced).

python -m pytest -q
298 passed, 2 skipped in 17.37s

git diff --check
No whitespace errors.
```

The two skipped tests are the explicit disposable-PostgreSQL concurrency
checks; their reason is reproduced below and they are not counted as passed.

```text
python -m pytest tests/integration/test_processing_postgres_concurrency.py -q -rs
SKIPPED [1] ... Run with --ds=config.settings.postgres_test and PHR_POSTGRES_TEST_URL
SKIPPED [1] ... Run with --ds=config.settings.postgres_test and PHR_POSTGRES_TEST_URL
2 skipped in 0.69s
```

## Chrome upload interaction — RUN

```text
python -m pytest tests/browser/test_ac02_upload_browser.py -q
1 passed in 6.90s
```

The installed Chrome run exercises drag/drop and file selection, per-file
failure recovery, exact and possible-duplicate behavior, durable local object
creation, task cards, and the 1280/1024/1023 desktop layout boundaries with no
browser console errors.

## External checks not run

- Live S3-compatible object storage: **NOT RUN** — no endpoint or credentials
  are configured. The boto3 **1.43.83** adapter contract is covered with a fake
  S3 client, but live bucket policy, encryption, versioning, and streaming are
  still release blockers.
- PostgreSQL concurrency: **NOT RUN** — `PHR_POSTGRES_TEST_URL` is unavailable;
  both dedicated concurrency tests remain skipped and are a release blocker.
- Redis/Celery multi-process continuation: **NOT RUN** — no external broker or
  worker is available. Durable queued state, retry scheduling, stale recovery,
  and new-session status continuity are covered in-process, but a deployed
  worker continuity run remains a release blocker.
- Edge and Safari: **NOT RUN** in this Windows environment. Chrome passed; the
  remaining supported-browser matrix is a release blocker.
