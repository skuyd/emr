# 癌种候选与指标排序验证

本记录对应[癌种排序规格](../specs/2026-09-08-cancer-candidates-and-ordering.md)、
[实施计划](../plans/2026-09-08-cancer-candidates-and-ordering.md)及五批 B3-04。
历史应用检查点通过合成验收与完整源码独审，已提交[草稿 PR #73](https://github.com/skuyd/emr/pull/73)；尚未合并或发布。
类型化病理适配及后续实际云影像、病灶主线合流均已有分范围独审；当前源码 `c7fd340`
仍待交付文档独审和最终准确 PR 头 CI。第二次真实资料评估未启动。
不能由历史批准或本记录推定新修订、真实质量或五批整体完成。
历史机器证据见[验证摘要](artifacts/cancer-ordering.json)，其原字节保持不变。
本轮独立的[类型化病理作者摘要](artifacts/cancer-typed-pathology.json)按源码检查点记录新执行，
不覆盖历史摘要，不代替最终独审、主线合流或 CI。
后续实际完成的审查、合流及原失败见[联合交付摘要](artifacts/cancer-joint-output-delivery.json)
与文末新记录；旧摘要中当时的待办状态保留原字节，不是当前状态。

## 应用范围与验收映射

本节历史应用源码检查点为 `f0fee68dd9004b41d32b70e2cb6c45822d18f217`，
已包含实际主线 `9675f0e3f61f96eb4895c364229b9da9d8a27bdb`。
此次交付准备在原功能分支继续，不从其他未合并功能创建新基线。

| 要求 | 当前实现及直接验证入口 | 尚需保留的边界 |
| --- | --- | --- |
| 报告明确表述产生候选 | `matching.py`、`narrative_layout.py`、`narrative_matching.py`；`test_matching.py`、`test_narrative_matching.py` | 只支持规格固定五字面；否定、疑似、历史、他人和未知保持独立，不推导系统诊断 |
| 原文及来源完整性 | `sources.py`、`narrative_sources.py`、`extraction.py`；`test_sources.py`、`test_narrative_sources.py`、`test_narrative_generation_review.py` | 原 Fact/OCR 不覆盖；原位置身份与当前父决定分开，不能生成干净副本绕过排除或更正 |
| 确认、更正与显示选择 | `services.py`、`views.py`；`test_services.py`、`test_manual_correction_review.py`、`test_narrative_views.py` | 显式选择覆盖 AUTO；候选确认不隐式确认父 Fact，撤销不恢复过期自动资格 |
| 自动及手动稳定排序 | `profiles.py`、`readmodels.py`、检验/趋势调用点；`test_profiles.py`、`test_lab_ordering.py`、`test_joint_order_submission.py` | 只重排；原值、完整多重集、单位规则、可比性和显式联合图顺序不变 |
| 选定输出和有限分享 | `exporting.py`、`output.py`、导出与分享适配；`test_export_selection.py`、`test_output_formats_and_sharing.py`、`test_narrative_consumers.py` | 默认不带新正文；来源类型交集、白名单、失效和流中断仍须成立，不外带未选原文或私有依赖 |
| 多用户、并发及生命周期 | `tests/integration/test_cancer_*_postgres.py` | 实际提交的撤权、作者/父变化、租约及流清理不能用 SQLite 或 mock 代替 |
| 桌面和手机操作 | 四份 `tests/browser/test_cancer_*_browser.py` | 实际上传、核对、排序、输出和撤销；浏览器样例为合成数据 |
| 旧数据与相邻领域兼容 | 癌种迁移测试及家庭迁移、影像、血糖、治疗导出回归 | 新来源不重写旧事实、偏好、导出/分享身份，也不修改已发布数组意义 |

上述源码路径均位于 `apps/cancer_ordering/`，未注明前缀的普通测试位于
`tests/cancer_ordering/`。矩阵是验收定位，不把实现文件存在当作测试通过。

## 已有分阶段证据

下表记录先前实际执行与独审的范围。测试集合有重叠，不能累加成新的全功能覆盖数字，
也不能将多个有界批准直接拼成整个分支获批。

| 检查点 | 已有证据支持的结论 |
| --- | --- |
| 原选定输出与分享 Task 6 | `APPROVED_BOUNDED_TASK_6`，无未关闭 P1/P2 |
| A 叙述边界修复 | 172 项通过，限定叙述来源问题关闭 |
| B 来源世代修复 | 父依赖变化后的旧决定绕过问题关闭；普通及 PostgreSQL 检查已执行 |
| C3.1 输出生命周期 | 29 项普通、10 项独立合同及 36 项 PostgreSQL 检查通过 |
| C3.2 浏览器 | 历史应用检查点 `38d5447` 的 SQLite/PG 各 4 个流程节点、另各 1 个父项完整性探针通过并目视核对；不是后续修复提交的新运行 |
| C4 评估工具来源证明 | 117 项纯合同、8 项 PostgreSQL、4 项 CLI 通过，仅支持工具修复，不是应用或真实执行批准 |

历史失败、修复和独审回执保持原样，摘要及哈希在机器证据中关联。

## 历史 f0fee68 合成验收

Windows 更新后的环境修订 v8-r2 已有 193 项作者和 45 项独立合成检查通过，但其中
应用调用数为 0，不能据此声称当前应用已经重跑。本轮在新的执行目录做应用验收，
不改原封存包、旧运行数据库、启动器、金标、评分器或历史独审证据。

普通应用、相邻导出和权限/迁移回归使用 `config.settings.test` 的内存 SQLite；
PostgreSQL 与浏览器使用新的专用合成测试环境。每批结果按实际命令、退出码、通过/失败/
跳过数量分别登记。已实现应用的完整源码独审及修复增量复审已通过；当前环境的
PostgreSQL/浏览器验收也已实际结束，三批均退出 0，源码清单在执行前后无变化。

| 最终应用检查点 `f0fee68` | 通过 | 失败/错误/跳过 | 耗时 |
| --- | --- | --- | --- |
| PostgreSQL 七份癌种集成测试 | 90 | 0/0/0 | 646.002 秒 |
| PostgreSQL 四份癌种浏览器测试 | 9 | 0/0/0 | 529.854 秒 |
| SQLite 同四份浏览器测试 | 9 | 0/0/0 | 360.177 秒 |

两份 PostgreSQL 批次使用各自专用合成基库及测试库，结束后已移除且无遗留连接；
SQLite 使用进程内存库。测试目标、设置、JUnit、执行回执及源码清单哈希在机器证据中逐批记录。
954 个源码文件绑定未变；浏览器保留 172 张截图，独立测试代理目视复核 12 张代表性截图通过。
最初旧测试服务不可用的尝试在 pytest 启动前终止并保留记录，随后使用新建合成服务完成上述三批。
新服务已正常停止，审计数据保留；旧服务、封存证据及真实资料均未修改。

初始 `38d5447` 的普通应用与相邻回归 **447 项通过、0 跳过**，耗时 394.60 秒。
随后完整差异独审另外复现两个 P2：未知候选标签外带完整原始分句，以及长标签生成后
无法被本系统 portable reader 读回。这说明 447 项通过没有覆盖全部输出边界。
原独立红例保持；修复使用“明确拒绝不安全自动标签 → 实际核对入口 → 人工更正标签 →
完整输出成功”，永久新增的 6 项反例也已在修复前实际失败。私有输出策略版本递增，
旧快照须失效；候选原稿、身份及未知配置保持不变。修复后首轮定向检查 77 项通过、1 项
失败，失败是新增测试预期 302 而项目成功更正使用 303；只修正测试预期后，新增 6 项完整
通过（24.84 秒），包含实际核对更正与 JSON/CSV/PDF/分享投影。原失败结果保留。
独立修复复审和 PostgreSQL/浏览器验收另行登记，不能把修复前 447 项声明为最终代码的全量结果。

完整源码审查范围为 `9675f0e3…f0fee68` 的 113 个文件，两个 P2 经实际修复复验关闭，
该检查点审查的 P1/P2 为 0。独立修复执行共 **15 项通过、0 跳过**（56.19 秒）：新增独立 9 项及
复跑永久测试 6 项，后者与作者执行重叠，不另算功能覆盖。它覆盖 Fact/叙述两类来源、
不同断言/对象的拒绝、更正及实际接收者分享，同时验证旧策略的预览、worker、下载、后续
数据流和分享均拒绝并清理快照。原两项独立红例不修改，也不重标通过；独立新探针首次
误用预览路径的失败保留，改正该探针路径后完成复验。源码审查不包含后续真实评估批准。

公开文档独审绑定 `20ae839`，31 项执行结果、源码与证据哈希对账通过，无未解决发现；
回执哈希在机器证据关联。此后仅补充实际草稿 PR、CI 观察及本回执引用，未改变应用源码或测试结论。

## 历史真实质量及未完成项

只有旧检查点 `f732e6342e5539e85e5aa24bc458b9b7809bcdce` 完成了第一次固定开发集执行。
它使用既有 OCR 回放并实际持久化；没有重新测量 OCR 或图像准备质量。
原公开结果为 **TP 1、FP 0、FN 10，完整表达正确 0/11**；命中项的断言和作用对象来源
未核实。共 64 文件、124 页，94 页完成固定字面范围核对、30 页部分核对；96 个标注发生
包括 11 个目标、84 个明确负例及 1 个未知。未判断区域继续保留。

64 个独立合成来源容器观察到的默认配置均为 GENERAL；这不是患者分组或诊断准确率。
该集合是固定开发集，不是独立留出集。旧结果不能用于宣称当前叙述来源修复已有真实改善。

第二次真实执行及逐项 retained/lost/new 比较未完成，本轮未启动该执行。
病理 PR #70 已实际以 `4b73d2e9c925ef48c95c217c7c055c5b7157e13b` 合入主线；
`specimen.histology` 适配据此在原癌种分支开发和单独验证，进展见下节。
现有 EXCERPT 与叙述来源的历史批准不能替代这项范围。

完整分支审查、所需 CI 和上述未完成范围分别跟踪。生产门禁仍为 **BLOCKED，8/23 通过**；
文档/门禁文件校验通过不代表生产放行，也不构成真实资料评估授权。

草稿初始文档提交 `20ae839` 的 [CI 运行](https://github.com/skuyd/emr/actions/runs/34465719697)
四项检查均未实际启动：GitHub 返回账户付款失败或支出上限需调整，runner 为 0、步骤为空。
这是当时的外部阻塞记录；之后仓库公开后的 CI 与新修订分别跟踪，不能以本地通过替代所需 CI 或据此合并。

## 类型化病理适配作者修订

本轮从原分支正常合入实际病理主线 `4b73d2e`，后续正常合入实际治疗修复及自动版本主线
`5102c099`，形成 `cd66b8a3` 冻结检查点。仅采用已经合入主线的接口，不导入未合云影像、
病灶或完整分子功能。旧公开 JSON、封存包、独审与真实结果均保留。

严格来源适配、READY 到 PUBLISHED 的稳定输入身份、同位置 EXCERPT 去重与旧审核障碍、
选定输出和未选上下文失效均有新增合成回归。新增 `OccurrenceReview` 迁移只复制已经存储的
候选审核及原位置快照到附加历史表，不重写旧候选、事实或选择，也不读取原件、运行抽取或真实评估。
这是后续历史保留迁移；前述限定叙述初始迁移的“只建空表”描述保留其当时范围。

`cd66b8a3` 独审实际复现历史作者失效后无法重新核对恢复，以及最后来源读取期间撤权后，
错误表单仍释放私有正文两个 P2。原红例保留；作者修订要求当前有效作者合法，完整历史继续
参与身份，历史失效只要求当前明确核对；同时将最终权限检查放在最后来源读取之后，私有详情
显示实际报告及直接绑定标本。31 项定向合成回归已通过，包括真实导出/分享错误表单路由；
同修订的 PostgreSQL 正常提交边界及浏览器共 16 项通过、零跳过。作者又实际复现损坏的
跨患者报告关联使详情带出外部报告身份的问题；该修订的通过不覆盖这项新红例。

原 `cd66b8a3` 冻结全仓普通回归为 3910 项通过、2 项因 Windows 符号链接权限跳过，
PostgreSQL 为 277 项通过、零跳过；两批源码前后未变。它们属于修复前检查点，不将结果
改记为后续作者修订通过。作者 `d4f3b7db` 的整个癌种模块回归随后以 443 项通过、零跳过
结束，源码前后未变；它仍属于上述显示范围修复之前，不能覆盖后续修订。

后续修订只接受同文档、同解析版本且与字段实际声明上下文一致的报告，类型化叙述父源
提前走同一严格适配器。关联缺失或错误时保留不可用来源身份，既不读取无关报告图，也不
生成缺失报告链接。三个实际 HTTP 原红分别覆盖跨患者、同患者跨文档及同文档错误报告；
首次 NULL 外键尝试被数据库 CHECK 拒绝，是探针构造失败，未计为应用反例。另在原作者
检查点实际复现“合法外键但声明的报告上下文不存在”一项红例。修复后的范围、合法同文档
报告/直接标本及真实输出复验为 12 项通过，另一次重叠检查为 58 项通过及 1 项前述构造失败。

| 类型化范围 | 直接回归入口 |
| --- | --- |
| 严格字段、角色、字面、来源位置和完整置信度 | `test_typed_pathology_sources.py`、`test_typed_overlap_context.py` |
| 历史作者重新核对与当前无效作者拒绝 | `test_typed_author_recovery.py` |
| 同位置审核保留、未知页范围和附加迁移 | `test_typed_occurrence_history.py`、`test_typed_unlocated_history.py`、`test_typed_review_boundary.py`、`test_occurrence_review_migration.py` |
| 实际 worker 发布身份及抽取库存 | `test_typed_pathology_pipeline.py` |
| 实际私有报告/标本身份与损坏关联 | `test_typed_pathology_views.py`、`test_typed_report_scope.py` |
| 输出白名单、父变更和最后权限核验 | `test_typed_pathology_outputs.py`、`test_typed_late_permission.py`、`test_typed_output_late_permission.py` |
| 正常 PostgreSQL COMMIT 与浏览器 | `tests/integration/test_cancer_typed_pathology_postgres.py`、`tests/integration/test_cancer_typed_late_permission_postgres.py`、`tests/browser/test_cancer_typed_pathology_browser.py` |

未注明目录的测试位于 `tests/cancer_ordering/`；各批有重叠，不累加为独立覆盖数量。

该阶段作者源码冻结于 `e846263303e62d0a2c9a3de4de44c0f87b152282`。此源码的 PostgreSQL
14 项正常提交/输出流边界及手机、桌面浏览器 2 项均已通过，共 16 项、零跳过，执行前后
源码清单未变；其结果不沿用前一修订。两种宽度的实际报告/标本显示已目视核对，严格浏览器
网络与控制台诊断保留。文档、Django 和迁移一致性检查通过。

该作者检查点尚未合入后续实际云影像输出主线。最终合流必须保留该主线已发布的全部
portable 数组，并验证后续格式；不能把此处独立通过当作最终格式兼容批准，也不能提前
导入尚未合并的病灶或完整分子分支。

上述作者记录形成时，最终源码独审、精确提交 CI 和后续实际主线合流尚待完成；后续实际
进展见下节。生产门禁保持 BLOCKED；第二次真实资料评估为 NOT_RUN，不以合成通过、
更新哈希或 GitHub Release 启动真实评估。

## 实际主线联合输出与交付准备

当前已审源码为 `c7fd34098992ad14c49c2227fab6b04700a5ba87`。新机器制品
[联合输出与交付摘要](artifacts/cancer-joint-output-delivery.json)连接以下阶段，旧两个癌症
JSON、原环境封存、原失败和独审原件全部保留；没有用新摘要覆盖旧结论。

| 阶段及作者归属 | 实际结果与范围 |
| --- | --- |
| Windows 环境 v8-r2，独立环境审查者复核 | v7 的 5 个 DLL 无变化且匹配，后续环境清单、启动及 launcher 绑定、封存依赖重新生成；193 项作者与 45 项独立合成检查通过，严格读回 1061 个文件。应用调用为 0，不能用这些数字或更新哈希证明应用通过；该历史回执的 v7 功能计划 555 项记为未执行，后续应用运行各自登记，不能追认这份原计划；环境修订未创建真实执行授权 |
| 类型化修复 `e846263` / 文档 `902b65c`，根代理非作者审查 | 66 项类型化永久回归通过；原探针 7 通过、2 个错误字段查找失败保留，按实际候选 ID 修正后 2 项通过。历史作者恢复、最后权限核验和实际报告/标本身份边界关闭，未关闭 P1/P2 为 0；不覆盖未来主线合流 |
| 云影像主线合流 `d0046c8`，根代理编写合流、功能作者仅审该增量 | 53 项定向通过，原 52 通过/1 个 schema 说明文字断言失败保留；独立 23 项通过加 1 个 ShareForm 探针入口错误，改走实际 create_share 后 2 项通过，另 2 个末次权限探针通过。普通 1046 项通过；PG/浏览器 149 项中 148 通过、1 个旧 1.5 预期与实际 1.6 不符，原失败不重标通过 |
| 病灶主线合流 `8ed2e29`，功能作者编写 | 正常合入实际 `feca010`，其病灶功能依赖已有主线证据；仅实际已合分支进入组合。便携 1.7 保留癌症、病灶、云影像、IHC 与全部旧数组，旧 1.6 非空病灶/云可读、旧非空癌症拒绝 |
| 浏览器后续 `c7fd340`，同一功能作者修订 | 仅一个新浏览器测试文件变化，改为模块导入以免误收集两个旧用例；等待 DOMContentLoaded 及真实初始状态 200，实际病灶改名后等待状态 410 并检查全部旧正文清空。准确提交上 PostgreSQL 手机测试 1 项通过、0 跳过；应用源码未改 |
| 当前联合增量独审，另一审查代理 | 独立核对三方 27 个非自动路径、相关自动合流、`c7fd340` 唯一测试增量及全部作者原件。组合、旧格式、三域最后渲染失效和路由审计共 8 个案例最终通过；首轮 6 通过/2 个审计探针错误保留，改用实际哈希审计字段后 2 项通过。未关闭 P1/P2 为 0，仅批准该增量，不自行批准其曾编写的其他旧功能 |

当前作者 80 项定向运行开始时 HEAD 仍为 `d0046c8`，但合流已经 staged，实际索引树为
`db5da1fc4a284d8f8aa84cda8730b6accb441a16`，1474 个受跟踪文件在执行前后不变。
它与 `8ed2e29` 仅差计划段落及后加入的两个 PG/浏览器测试文件，应用、模板、静态资源
和配置字节完全相同；不能把它写成干净 `8ed2e29` 的一次测试。最终头保留这种明确继承。

`8ed2e29` 的 3 项历史迁移全部通过；另冻结组为 **20 通过、1 失败、0 跳过**，其中 15 项
真实 PostgreSQL 提交边界全部通过，6 个浏览器用例中包含误收集的两个旧 Harness 用例。
唯一失败是新混合手机导航等待 networkidle 超时。同源独立单节点观测再次失败并保留：
导航约 15.9 秒已得到主文档 200 和 DOMContentLoaded/load，后台状态请求也返回 200，
但网络空闲未成立。这是等待条件诊断，不是响应性能达标结论，也未提高全局超时。
后续 `c7fd340` 的单项通过关闭该测试问题；不把原 21 项失败运行改记为全通过。

当前主线病灶源码来自已合 `feca010`；此前 `c0ba86f` 的云/病灶合流范围及独审仍见
[病灶联合证据](artifacts/lesion-cloud-main-integration.json)，不将其测试数字累加为癌症新测试。
癌症父与主线父的 148/149 个验证 JSON Git blob 均保持；当前文档修订前共有 151 个旧验证
JSON。当前 1476 个文件按 NUL 路径清单逐项绑定，最终浏览器运行前后字节一致；跨 worktree
CRLF/LF 差异按 Git blob 核验，不冒称跨检出原始字节一致。独立测试服务已正常停止，日志保留。

作者联合回执 SHA 为 `cd2ab451a8ab9846aa8475a50d47f588ff7393e42a338dccd7930c7f8c1c9485`；
当前源码/测试增量独审回执 SHA 为 `61905f001faf6f853d5146abc201ffec7b475fdc54736fc4583fc481b4d0d849`。
各运行 XML、日志、原失败及较早独审的 SHA 在新机器摘要中列出。上述集合有重叠，不加总为
一次全功能验收；本次文档由联合输出作者编写，尚待根代理独立审查。

PR #73 仍是未合并草稿，最终交付头 CI 尚未运行，`releases: []`；不预填自动发布版本或
使用其他 PR 的通过结果。固定五字面与仅显示顺序范围不扩大，第一次真实开发集的 TP 1、
FP 0、FN 10、完整表达 0/11 仍是唯一该集结果；真实第二次评估 `NOT_RUN`，生产仍 BLOCKED。
