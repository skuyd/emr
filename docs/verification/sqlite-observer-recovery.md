# SQLite 测试观察器竞争恢复验证

[v1.17.0](../releases/v1.17.0.md) 功能主线 `7b9fd7c` 的 CI34510391095 曾有
**42 失败、3502 通过、4 跳过**。原匿名 JSON、失败节点和日志保持；本记录追加后续恢复事实。
[恢复制品](artifacts/sqlite-observer-recovery.json)逐条绑定原 42 节点、当前测试函数和 CI 选择，
[v1.18.0 发布制品](artifacts/release-v1-18-0.json)记录精确成功、取消及待运行身份。

## 已证实修复与实际验证

在真实 HTTP 响应事务尚未收尾时，独立测试观察器读取共享内存 SQLite，作者实际复现表锁。
`8735ea4` 仅修改 `tests/browser/sqlite_server.py`、`test_glucose_browser.py` 与新增
`test_sqlite_database_actions.py`：观察器的 ORM 工作使用既有 WSGI SQLite 锁，覆盖实际 worker
完成，正常和异常路径都释放锁。PostgreSQL 继续没有这把 SQLite 请求锁；没有改变应用业务、
浏览器断言、指标选择、授权或来源有效性规则。

| 原执行 | 实际结果 | 边界 |
| --- | --- | --- |
| 原竞争复现 | 1 项实际表锁失败 | 保留原 RED，不重标通过 |
| 作者 SQLite | 49 通过、0 跳过 | 受测暂存源码与最终 `8735ea4` 一致 |
| 作者 PostgreSQL 设置 | 7 通过、0 跳过 | 6 项实际血糖浏览器流程 + 1 项无数据库配置并发控制，不是 7 项 PG 锁测试 |
| 非作者独审 | 15 通过、0 跳过 | 实际 SQLite HTTP/收尾、观察器异常及并行配置；未另跑真实 PG |
| [PR #83 CI34519218423](https://github.com/skuyd/emr/actions/runs/34519218423) | 普通3546通过/4跳过、PG215、必跑浏览器18、JS9通过 | 精确 `b44a367` |
| [主线 CI34521499934](https://github.com/skuyd/emr/actions/runs/34521499934) | 普通3546通过/4跳过、PG215、必跑浏览器18、JS9通过 | 实际 Squash `3e43e59` |

独审无未关闭 P1/P2；其回执及原 RED/绿测 XML 哈希在恢复制品中关联。测试服务正常停止，
旧运行材料保留。PR #83 已合主线，本次 [v1.18.0](../releases/v1.18.0.md)包含该测试修订。

## 42 项原失败怎样闭合

原日志的观察类别为14项表锁、17项断言不匹配、3项事务守卫及8项事务内迁移错误。
目前只对已复现的观察器竞争建立因果修复；没有声称全部42项共享同一根因。
当前普通 CI 命令仍选择42个原失败函数，没有将其移到排除路径或 PostgreSQL/OCR 标记。
修复后原普通范围在 PR #83 与实际主线完整通过，随后实际病灶主线及本次发布候选更大
范围也完整通过。因此已复现问题与受影响选择范围的恢复验证已闭合；不是凭更新哈希，
也不是把旧失败记录改成通过。若后续再次出现级联现象，须保留新失败并重新定位。

各次测试集合重叠，不累加为覆盖总数。最后癌种主线因新发布提交而取消，新发布提交
主线 CI 当前仍待终态；不能把候选通过或旧成功运行替代它。历史未知根因范围仍明确保留，
不影响已有修复证据的准确归属。

本记录不改变云二维码、病灶、癌种真实质量评分，不启动真实资料评估或生产访问。
