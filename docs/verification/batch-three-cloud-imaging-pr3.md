# 第三批云影像选定输出与限时分享验证

云影像 PR 3 已实现独立来源选择、PDF/JSON/CSV/ZIP 输出及限时来源访问，已完成作者主线合流
和本地冻结验证，等待非作者复审及精确 PR CI。初始基线为 PR 2 实际 Squash 合并提交
`61dbbc8835702bd998272a036152ea2ba466cede`；当前作者提交 `ecb0932`、修正 `084e29a`，
第一阶段审查头为 `d55abf0`，三项 P2 的作者修订为 `82ad10e`。冻结头
`89909da5580093233b9034fbfe1304a07fd68f2d` 已整合病理功能实际 Squash 主线
`4b73d2e9c925ef48c95c217c7c055c5b7157e13b`。尚未创建本功能 PR、发布或放行生产。

该主线实际可移植 writer／reader 仍为 `1.4`，病理语义由既有临床数组承载；云来源新增
两张独立数组的本地格式因此仍为下一版本 `1.5`，保留 `1.0`—`1.4` reader 及病理语义校验。
病理和云来源选择页的最终状态检查均保留；混合输出测试使用实际 IHC、日常、血糖、
检验、治疗和云来源数据核对各表。应用版本由自动发布流程管理，本记录不预定功能发布版本。

## 选择与输出

| 维度 | 实际边界 |
| --- | --- |
| 云来源选择 | `selection.cloud_source_ids` 默认空，普通资料、影像章节或报告选择不隐含来源许可；表单提交同时绑定显示时的来源令牌 |
| 来源独选 | 文档集合可以为空。私有依赖仍包含原件、页、证据、报告状态、当前及历史作者、来源修订和规则身份 |
| 混合选择 | A 原件和 B 来源可以共存，实际 ZIP 仅包含 A 原件字节。B 的来源标识不授予 B 原件访问 |
| 报告与字段范围 | 来源必须属于明确报告范围与字段报告范围的交集。空范围、未归属来源及任一无效 ID 整体拒绝，不静默丢弃 |
| 可移植关联 | 独立 `cloud_imaging_sources` 和 `cloud_imaging_evidence` 表保留来源／修订、原件／页／报告身份；二维码无 OCR 偏移，原始载荷、历史地址及整页文字不导出 |
| 地址保护 | 仅选定来源行的 `current_url` 保留原始语义；其余 URL 继续省略。CSV 公式防御、PDF/HTML 转义及离线表结构／关系验证覆盖新内容 |

选项旁说明下载文件可能包含访问参数。来源独选不生成虚构 Document，不把私有清理索引
当成公共文档集合。旧格式缺少新表时缺省为空，所有实际已发布领域数组与验证必须保留。

选择表单显示脱敏的原件名、标题、页码、来源类型和按建立顺序排列的页内来源序号；
短来源标识发生前缀碰撞时延长。每项另有内部来源核对入口，可继续查看准确原页。
同站点、同页码、同修订及同名的来源仍可区分，完整地址和访问参数不进入默认选择表单。
相似来源测试同时覆盖不同原件和同一原件同一页，并验证只导出／只分享实际勾选的一项。

## 精确分享与失效

分享快照仅显示安全站点、来源类型、修订及必要页／报告身份，不含原地址、二维码载荷、
作者账号或历史。接收者必须通过实际分享令牌交换，并绑定自己的账号、登录会话及期限；
创建者身份、成员修订和资格同样持续核验。

分享 GET 说明页和 POST 打开路由位于 `/shared/<share_id>/cloud-imaging/<source_id>/`。
目标仅从当前真来源绑定解析，不调用家庭 READ 代替分享授权。原件另按明确文档许可检查，
仅有来源分享的接收者不能访问完整原件、普通私有来源或其他未选来源。

来源、证据和文档使用真 FK 与冻结身份。分享 scope、快照选择与实际绑定必须完全一致；
删除、增加或丢失一项均使旧分享整体失效并清空快照。排队、worker 构建、存储发布、
预览渲染、跳转响应、下载 IO、首字节和每个读取块前后均保留重新核验。存储锁期间的
正常来源写入须等待锁释放；提交后下一次输出必须被阻止。

来源独选导出在私有文档清理索引中登记，来源独选分享不加入 `ShareSource`。实际非空
导出对象随原件硬删除被发现和清理；存储失败保留重试状态，重试成功后才删除原件聚合。
回收站恢复、撤销决定或重新登录不会恢复已失效输出。

## 已观察的测试与修订

下列是实施期间的有界事实，运行范围重叠，不相加，也不替代最终冻结头验证。

- 相关完整回归曾为 553 通过、5 个 PG 项目排除；该运行后继续追加范围和异常保护修订。
- 专用 PostgreSQL 27 项通过，另有 2 项真实跨领域部分作者清理提交及 1 项来源 FK 单独
  更新等待用例通过。均使用独立合成测试库，最终全套尚待重新冻结。
- 实际 PDF 文本、JSON/CSV 表、ZIP 清单和原件字节已验证；同页 OCR/QR 来源、报告／字段
  范围、Unicode／编码路径／重复查询参数及 CSV 公式文本有永久回归。
- 中途审查指出分享 scope 云来源集合未与快照比较，已补整体失效检查和新增／移除 ID
  的真实 HTTP 反例，修订后相关 45 项定向测试通过；仍需最终独立复审。
- 作者异常报告测试最初 2 失败、1 通过，确认 `/visit/` 的异常链可能带出合法选中地址。
  现对导出入口应用私有报告投影，保留异常类型、链结构、堆栈和请求身份；共享云入口
  沿用早期保护。修订后 35 项定向测试通过。
- 随后的冻结 TLS 桌面／手机测试各失败一次：新中间件错误地覆盖导出页面为
  `no-referrer`，原生表单因此提交 `Origin: null` 并被真实 CSRF 拒绝。作者修订 `084e29a`
  恢复导出的 `same-origin`；外部受控打开继续 `no-referrer`，未放宽 CSRF。实际两尺寸
  TLS 和三项异常报告复测共 5 项通过、零跳过。
- 非作者对 `d55abf0b8967996a2778873a5e91d4b75cb3da8c` 的完整 42 文件第一阶段审查
  发现三项 P2、无 P1：分享创建页异常报告缺口（真实链反例 1 失败）、相似来源可见标签
  重复（真实表单选项反例 1 失败）和 TLS 空窗口关闭观测竞态。旧回执摘要
  `a1fbf34a240ec874c865552410d47cdd14f1c8d52fa689dfad61c6ac3bab00fc` 保留；它不是最终批准。
  新作者修订将分享 GET／POST 纳入早期异常报告保护，保持原生 CSRF；增加上述选择线索
  和原页核对入口；浏览器有限等待实际关闭事件并继续断言空窗口已关闭。
  第一轮五项异常报告、三项来源选择和两项真实 TLS 共 10 通过；之后扩展同页精确选择，
  五项选择测试通过。初版标题／文件名测试误改不可变文档字段的失败同样保留，测试改为
  正常支持的来源标题更正后通过。随后完整云来源、导出视图、家庭分享和两尺寸实际 TLS
  定向回归为 263 通过、2 跳过（这两项要求专用 PostgreSQL，普通配置未执行），耗时
  224.83 秒；手机选择表单截图也已检查无横向溢出。这些合流前作者结果仍须最终冻结
  验证及非作者复审，不能替代专用 PostgreSQL 运行。

该失败头 `892a96a` 的两项长测试主动中止，分别保留 `interrupted_not_a_pass`、执行日志和
执行前后源码不变证明；不把中止或更新哈希记为测试通过。原失败与之前所有封存 JSON、
PR 1/PR 2 独审和真实识别证据保持原样。本轮新增匿名测试和身份制品，旧 130 份制品的
Git blob 与第一阶段独审回执不变；非作者复审尚待完成，不把作者修复当成独审批准。

## 合流后的冻结作者验证

以下三次运行均绑定完整提交 `89909da5580093233b9034fbfe1304a07fd68f2d`，逐份比对所有
tracked 文件的测试前后 SHA256，三次清单一致且无文件变化。测试结果和源码身份分别见
[匿名测试摘要](artifacts/batch-three-cloud-imaging-pr3-tests.json)和
[源码身份记录](artifacts/batch-three-cloud-imaging-pr3-identity.json)。

| 实际范围 | 终态结果 | 耗时 |
| --- | --- | --- |
| 全部 cloud_imaging、exports、patients、operations 普通测试 | 641 通过，0 跳过；5 个 PG 标记项目排除 | 721.358 秒 |
| 全部选定云输出 PostgreSQL 生命周期与独立正常提交测试 | 34 通过，0 跳过 | 168.720 秒 |
| 桌面／360px 真实 TLS Chromium | 2 通过，0 跳过 | 49.763 秒 |

合流前另有 40 项病理兼容、reader、选择视图和相似云来源测试通过，不能替代上述冻结结果。
本地实际环境为 Python 3.11.9、Django 5.2.17、PostgreSQL 18.6、Playwright 1.62.0；
完整包版本及二进制摘要已记录。本地 idna 为 3.11，精确 PR CI 仍须使用仓库锁定依赖
执行全仓普通、全部 PG 与必跑浏览器，不能把环境摘要更新视为该 CI 已通过。
本轮专用合成测试库已删除，PG 服务已停止；旧 runtime、失败日志和封存证据保留。

## 永久验证入口与限制

永久回归位于 `tests/cloud_imaging/test_selected_output*.py`、
`tests/integration/test_cloud_selected_output_postgres.py` 和
`tests/browser/test_cloud_selected_output_browser.py`。CI 单列两尺寸浏览器为必跑项目，
全部 PG 场景进入既有非空且无跳过的必跑选择。

浏览器使用真实本地 TLS，仅拦截合成 `.invalid` 目标；检查无自动外访、实际目标、
无跨站 Referer／opener，以及旧说明页修订后空窗口关闭。未接入真实医院地址，未运行
真实资料评估或上传私有材料。本地功能通过不建立第三方可用性、二维码覆盖质量或
医疗正确性；[PR 1 的真实质量限制](batch-three-cloud-imaging-pr1.md)继续有效，
[三 PR 规格](../specs/2026-09-08-cloud-imaging-sources.md)、
[实施计划](../plans/2026-09-08-cloud-imaging-sources-implementation.md)和
[生产门禁](release-gate.md)分别管理，五批总状态保持实施中。
