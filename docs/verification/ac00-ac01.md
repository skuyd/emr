# AC-00 / AC-01 verification record

Recorded at **2026-08-30T11:24:16Z** (UTC) on Python **3.11.9** and Django
**5.2.17**. This record contains no real phone number or patient name.

## SQLite acceptance and static-contract evidence — RUN

The acceptance test drives the public Django HTTP OTP flow, first onboarding,
three exact consent types, the P01 field/required-field whitelist, session
expiry with an encoded safe return path, and the database/service one-patient
invariant. It runs against the configured SQLite test database; it is not a
PostgreSQL contention result.

```text
python -m pytest tests/acceptance/test_ac00_ac01.py tests/deploy/test_compose_contract.py tests/deploy/test_bootstrap_dev_env.py tests/test_project_configuration.py -q
9 passed in 0.53s

python manage.py makemigrations --check --dry-run
No changes detected

python manage.py check
System check identified no issues (0 silenced).

python manage.py migrate --noinput
No migrations to apply.

python -m pytest -q
130 passed in 2.76s
```

The compose contracts statically verify pinned images, loopback-only ports,
named volumes, mandatory environment credentials, authenticated Redis, private
versioned MinIO initialization, and PostgreSQL `pg_trgm` initialization. The
development bootstrap test verifies that `.env` is exclusive-create only and
that its generated application URLs share the generated service credentials.

## Deployment check — RUN with documented test-environment warnings

```text
python manage.py check --deploy --settings=config.settings.test
```

This returned only Django's expected test-environment warnings:
`security.W004` (HSTS unset), `security.W008` (HTTPS redirect unset), and
`security.W009` (deliberately short test secret). No `phr.E001` through
`phr.E006` appeared.

## Runtime checks not run

- Docker CLI: **NOT RUN** — not installed; therefore compose startup, health
  checks, private-bucket initialization, Redis authentication, and MinIO
  versioning were not executed.
- PostgreSQL/`pg_trgm`: **NOT RUN** — `psql` and a PostgreSQL server were not
  available. No PostgreSQL concurrency/contention result is claimed.
- Browser interaction: **NOT RUN** — Edge and Chrome executables were
  detected at explicit local paths, but no manual/headless browser smoke was
  executed. The lead will run that separate check.
