# AC-00 / AC-01 authentication verification record

Recorded at **2026-09-01T19:07:45Z** (UTC) on Python **3.11.9**, Django
**5.2.17**, Node **22.15.0**, npm **10.9.2**, and local Google Chrome
**151.0.7922.175**. All authentication and patient values were generated
synthetic fixtures; no test credential is reproduced in this record.

## TDD migration evidence — RUN

The three legacy OTP-only nodes were first run unchanged:

```text
python -m pytest tests/acceptance/test_ac00_ac01.py::test_ac00_ac01_http_otp_onboarding_session_return_and_one_patient tests/browser/test_ac00_ac01_browser.py::TestAc00Ac01Browser::test_login_onboarding_and_safe_return_load_without_asset_or_console_errors tests/browser/test_ac02_upload_browser.py::TestAc02UploadBrowser::test_authenticated_user_uploads_a_real_image_without_console_asset_or_overflow_errors -q
3 failed in 67.58s
```

The acceptance node failed because the retired `/login/request-code/` route
returned 404. Both Chrome nodes timed out waiting for the retired OTP-only
button. After migration, no helper authenticates a normal login without both
password and a purpose-bound `SIGN_IN` OTP.

The new browser assertion for the brief's exact first-use action label failed
before the focused accessible label/field-ID and password-visibility fix, then
passed after the fix:

```text
python -m pytest tests/browser/test_ac00_ac01_browser.py::TestAc00Ac01Browser::test_first_use_login_rejections_safe_return_and_auth_viewports -q
1 passed in 13.16s
```

## Integrated authentication and dependent regressions — RUN

```text
python -m pytest tests/accounts/test_authentication.py tests/accounts/test_authentication_lock_order.py tests/accounts/test_authentication_postgres.py tests/accounts/test_flow_state.py tests/accounts/test_login_views.py tests/accounts/test_first_use.py tests/accounts/test_password_reset.py tests/accounts/test_otp.py tests/accounts/test_account_deletion.py tests/acceptance/test_ac00_ac01.py -q -rs
130 passed, 3 skipped in 9.40s

python -m pytest tests/accounts tests/patients tests/security tests/acceptance/test_ac00_ac01.py -q -rs
232 passed, 3 skipped in 13.50s
```

The integrated HTTP coverage verifies account creation only after verified
first use, in-place unusable-password account upgrade and ownership retention,
wrong-password rejection without OTP/session creation, password plus OTP login,
missing and expired `pending_mfa`, safe and rejected `next` values, neutral reset
responses on one verification surface, non-advanceable decoy reset state,
saturated password budgets, deterministic cross-flow lock order, terminal MFA
state cleanup, cross-session reset supersession with an indistinguishable
follow-up HTTP lifecycle and uniform secure session-cookie transition,
revocation of current and old sessions, safe reset return propagation into a
fresh login, exact materialization of both OTP mutex rows, generic rollback
when purge removes a waiting mutex, terminal delivery failure when a provider
callback purges its challenge, the single-patient invariant, and absence of
phone/password/OTP values from rejected session, response, and captured-log
evidence. The three skips are the optional authentication PostgreSQL contention
cases gated on an unavailable `PHR_POSTGRES_TEST_URL`; the deterministic
request, FIRST_USE, reset, purge, missing-mutex, and provider-callback
regressions ran and passed.

## Real Chrome journeys — RUN, no browser skips

```text
python -m pytest tests/browser/test_ac00_ac01_browser.py tests/browser/test_ac02_upload_browser.py -q
5 passed in 34.23s
```

The combined result is four authentication nodes plus one upload node. Chrome
executed first-use creation and onboarding, legacy upgrade, wrong and correct
password paths, password-to-MFA login, missing/expired MFA state, safe-return
routing, neutral reset, old/current-session revocation, safe reset return into
a fresh password-plus-MFA login, favicon asset and observer checks, and the
existing real-image upload/duplicate/processing journey. First-use and reset
started from visible login-page links and advanced through rendered forms; no
successful authentication journey navigated directly to an internal next-step
URL. The auth pages were checked at 390, 768, and 1440 CSS pixels with no
horizontal overflow.
Visible labels and `tel`, `current-password`, `new-password`, and
`one-time-code` autocomplete values were inspected. Native buttons toggled
password visibility by Enter and Space while maintaining `aria-pressed` and
`aria-controls`.

The runtime observer saw only the deliberately exercised authentication HTTP
400 responses, their one-for-one Chrome resource diagnostics, and the isolated
controlled-404 favicon observer regression. It recorded no unexpected 4xx or
5xx response, failed request or asset, page exception, or console error. No
screenshot or visual-baseline result is claimed by this task.

## JavaScript and TypeScript checks — RUN

```text
npm run test:js
3 passed, 0 failed

npx playwright test --list
Total: 27 tests in 1 file
```

The external release gate was not run because its base URL and fixture/storage
inputs were absent. Therefore no external Chrome/Edge/WebKit release-environment
result is claimed. The real local Chrome evidence is the Django live-server
suite above.

## Django and full-suite checks — RUN

```text
python manage.py makemigrations --check --dry-run
No changes detected

python manage.py check
System check identified no issues (0 silenced).

python -m pytest tests/acceptance/test_traceability.py -q
5 passed in 1.25s

python -m pytest -q -rs
772 passed, 6 skipped, 43 warnings in 76.71s
```

The six full-suite skips were five PostgreSQL concurrency tests requiring
`config.settings.postgres_test` plus `PHR_POSTGRES_TEST_URL` (three authentication
same-phone regressions and two processing regressions), and one prepared local
PaddleOCR model smoke test requiring `PHR_RUN_OCR_MODEL=1`. No PostgreSQL
contention or OCR model result is claimed. The warnings were the pre-existing
Requests dependency warning and Django warnings emitted by tests that override
`DATABASES`.
