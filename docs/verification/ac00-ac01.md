# AC-00 / AC-01 authentication verification record

Recorded at **2026-09-01T16:13:40Z** (UTC) on Python **3.11.9**, Django
**5.2.17**, Node **22.15.0**, npm **10.9.2**, and local Google Chrome
**151.0.7922.175**. All authentication and patient values were generated
synthetic fixtures; no test credential is reproduced in this record.

## TDD migration evidence — RUN

The three legacy OTP-only nodes were first run unchanged:

```text
python -m pytest tests/acceptance/test_ac00_ac01.py::test_ac00_ac01_http_otp_onboarding_session_return_and_one_patient tests/browser/test_ac00_ac01_browser.py::TestAc00Ac01Browser::test_login_onboarding_and_safe_return_load_without_asset_or_console_errors tests/browser/test_ac02_upload_browser.py::TestAc02UploadBrowser::test_authenticated_user_uploads_a_real_image_without_console_asset_or_overflow_errors -q
3 failed in 67.58s
```

The acceptance node failed because the retired `/login/request-code/` route
returned 404. Both Chrome nodes timed out waiting for the retired OTP-only
button. After migration, no helper authenticates a normal login without both
password and a purpose-bound `SIGN_IN` OTP.

The new browser assertion for the brief's exact first-use action label failed
before the focused accessible label/field-ID and password-visibility fix, then
passed after the fix:

```text
python -m pytest tests/browser/test_ac00_ac01_browser.py::TestAc00Ac01Browser::test_first_use_login_rejections_safe_return_and_auth_viewports -q
1 passed in 13.16s
```

## Integrated authentication and dependent regressions — RUN

```text
python -m pytest tests/acceptance/test_ac00_ac01.py -q
3 passed in 1.85s

python -m pytest tests/accounts/test_login_views.py tests/accounts/test_first_use.py tests/accounts/test_password_reset.py -q
65 passed in 3.01s

python -m pytest tests/accounts tests/patients tests/security tests/acceptance/test_ac00_ac01.py -q
209 passed in 5.78s
```

The integrated HTTP coverage verifies account creation only after verified
first use, in-place unusable-password account upgrade and ownership retention,
wrong-password rejection without OTP/session creation, password plus OTP login,
missing and expired `pending_mfa`, safe and rejected `next` values, neutral reset
responses, revocation of current and old sessions, fresh MFA after reset, the
single-patient invariant, and absence of phone/password/OTP values from rejected
session, response, and captured-log evidence.

## Real Chrome journeys — RUN, no browser skips

```text
python -m pytest tests/browser/test_ac00_ac01_browser.py -q
3 passed in 22.62s

python -m pytest tests/browser/test_ac02_upload_browser.py -q
1 passed in 7.27s

python -m pytest tests/browser/test_ac00_ac01_browser.py tests/browser/test_ac02_upload_browser.py -q
4 passed in 28.37s
```

Chrome executed first-use creation and onboarding, legacy upgrade, wrong and
correct password paths, password-to-MFA login, missing/expired MFA state,
safe-return routing, neutral reset, old/current-session revocation, fresh MFA,
and the existing real-image upload/duplicate/processing journey. The auth pages
were checked at 390, 768, and 1440 CSS pixels with no horizontal overflow.
Visible labels and `tel`, `current-password`, `new-password`, and
`one-time-code` autocomplete values were inspected. Native buttons toggled
password visibility by Enter and Space while maintaining `aria-pressed` and
`aria-controls`.

The runtime observer saw only the deliberately exercised HTTP 400 responses and
their one-for-one Chrome resource diagnostics. It recorded no unexpected 4xx or
5xx response, failed request or asset, page exception, or console error. No
screenshot or visual-baseline result is claimed by this task.

## JavaScript and TypeScript checks — RUN

```text
npm run test:js
3 passed, 0 failed

npx playwright test --list
Total: 27 tests in 1 file
```

An attempted `npm run test:e2e -- --list` did not forward `--list` in this npm
environment and launched the release gate. All 27 cases stopped because the
required external base URL and release fixture/storage inputs were absent; the
local WebKit executable was also absent. Therefore no external Chrome/Edge/
WebKit release-environment result is claimed. The real local Chrome evidence is
the Django live-server suite above.

## Django and full-suite checks — RUN

```text
python manage.py makemigrations --check --dry-run
No changes detected

python manage.py check
System check identified no issues (0 silenced).

python -m pytest tests/acceptance/test_traceability.py -q
5 passed in 0.67s

python -m pytest -q
748 passed, 3 skipped, 43 warnings in 69.68s
```

The three full-suite skips were two PostgreSQL concurrency tests requiring
`config.settings.postgres_test` plus `PHR_POSTGRES_TEST_URL`, and one prepared
local PaddleOCR model smoke test requiring `PHR_RUN_OCR_MODEL=1`. No PostgreSQL
contention or OCR model result is claimed. The warnings were the pre-existing
Requests dependency warning and Django warnings emitted by tests that override
`DATABASES`.
