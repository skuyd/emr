# 第五批日内血糖与选定输出验证

B5-02 已实现独立血糖记录、原件核对导入、日内曲线、多天时段表，以及选定速查、导出和
限时分享。本地相关回归、实际 PostgreSQL 与 Chromium 验证通过；来源、页面和输出的
分阶段独审已关闭确认的 P2。[功能 PR #63](https://github.com/skuyd/emr/pull/63) 已 Squash，
并随 [v1.13.0](../releases/v1.13.0.md) 发布源码。功能与发布 PR 的四项 CI 均通过；规格和计划
为 `implemented`，本验证记录的 `verified` 仅表示所列功能、CI 与评分事实已核验，真实质量
限制仍然存在。五批整体继续 `implementing`。

范围由[五批需求](../specs/2026-09-07-batches-one-five-requirements.md) B5-02、
[日内血糖规格](../specs/2026-09-08-intraday-glucose.md)及
[实施计划](../plans/2026-09-08-intraday-glucose-implementation.md)确定。
[本地验证制品](artifacts/batch-five-glucose-tests.json)保存精确提交、测试计数、失败历史与
独审身份；公开制品只包含方法、匿名计数及哈希。

## 已实现路径

| 用户路径 | 行为与边界 | 主要验证入口 |
| --- | --- | --- |
| 记录与更正 | 血糖仪自测、手动和原件来源分开；同分钟多次记录保留不同 UUID；初始值不可覆盖，更正、删除、撤销保留实际作者与历史 | `tests/glucose/test_payloads.py`、`test_services.py`、`test_history.py` |
| 检验导入 | 使用当前有效观察项和原页；核对血液葡萄糖及标本，不把尿糖、HbA1c、输液剂量或未来监测计划导入为测量 | `test_sources.py`、`test_source_names.py`、`test_source_context.py`、`test_source_boundaries.py` |
| 护理原件录入 | 明确选择本患者实际原件页，标为用户抄录；没有逐次时间的摘要不拆成多次测量 | `test_nursing_sources.py` |
| 时间与单位 | 保留原值、单位、秒或分钟、原时区状态和换算依据；没有印刷时区时不虚构 UTC；用户补时或确认时区记录为人工修订 | `test_payloads.py`、`test_forms.py`、`test_read_lifecycle.py` |
| 日内图与时段表 | 按真实间隔绘制，区分来源与标本；同刻点和一格多值全部保留；缺测、非单值、时间不详和未注明时段单独显示 | `test_history.py`、`test_display_timezone.py`、`tests/browser/test_glucose_browser.py` |
| 家庭与来源变化 | 旧页固定原患者；渲染后复核成员资格及当前/历史作者；来源更正、删除、重解析后旧确认失效，重新核对产生修订 | `test_views.py`、`test_read_lifecycle.py`、`test_lifecycle.py`、`tests/integration/test_glucose_sources_postgres.py` |
| 选定输出 | 只选血糖且无上传资料时也可生成 PDF、JSON/CSV/ZIP；混合检验、影像字段、日常记录及治疗派生内容分别保留选择范围 | `test_export_integration.py`、`test_output.py`、`tests/exports/test_treatment_exports.py` |
| 限定分享与撤回 | 分享只投影选定当前记录；不包含作者账号、完整历史或未选原文；更正、作者注销或撤权后旧预览、发布、分享及后续下载块均拒绝 | `test_output_lifecycle.py`、`tests/integration/test_glucose_export_postgres.py`、实际浏览器用例 |
| 迁移与清理 | 新记录及来源绑定不伪造上传文档；迁移保留既有日常记录、临床数据和旧导出/分享；作者注销保留其他家庭患者记录并置空作者 | `test_migrations.py`、`test_lifecycle.py`、既有家庭/分享迁移用例 |

时段必须有原件明确依据或用户选择，不按时钟或 `LAB_FASTING_GLUCOSE` 编码自动断言空腹。
原报告的日期、月份或时间未知状态不补成午夜。夏令时不存在的时刻拒绝；重复时刻要求
明确分支。只改数值时保留原秒值，改日期或时区时重新解析。

mg/dL 换算使用规格记录的 `value × 0.05551`，十进制计算、界面舍入和原值分开保存。
比较符、范围、HI/LO 或无法确认单位的内容保留原文及原因，不取中点或制造曲线值。
颜色表示显示数据中的相对数值，不设置疾病判断或治疗阈值。

## 选定范围与当前性

结构化格式从实际已合主线的 `1.3` 扩为 `1.4`，新增 `glucose_records` 与
`glucose_record_sources`，保留临床、日常记录和八张治疗相关表。读取器接受 `1.0` 至
`1.4`；旧文件缺失的新表按空数组读取，`1.4` 必须提供两张血糖表。

原件导入血糖的文档作为私有失效依赖参与锁定和指纹校验，不因此出现在选定资料表，
也不因此给分享接收者整文档权限。完整原件需要另行明确选择。只选血糖的限定分享
拒绝普通记录历史和原件路由；范围检查覆盖真实 JSON、CSV、ZIP 清单、PDF 文字及 HTML。

实际大文件测试生成 130 条合成记录的 JSON。收到第一块 256 KiB 后更正选定记录，
后续生产流停止；重复关闭流只记一次拒绝审计。混合日常记录/血糖作者注销的 PostgreSQL
检查在真实删除收集器更新部分数据、事务尚未提交时暂停，确认读取等待实际 COMMIT，
之后清空并拒绝失效快照。

## 本地验证与独立审查

输出集成冻结为 `85925fb07efa222ba67752b888ae951d99b48ab3`，预览修复为
`18331059b91e5400b113227f56bc751adcd20e15`。随后将实际主线
`5b668a1e9c900ee3abfb4e2be7cd65110cc9b4e4` 合流为
`aed0e6d7b523ca63b1503978924f88a3f7d59012`，只同步已发布版本与文档。844 份应用、配置、
静态文件、模板、测试和工具的 Git 身份与预览修复提交一致；Windows 换行差异另行记录。

| 实际执行范围 | 精确源码 | 结果 |
| --- | --- | --- |
| 血糖、导出、日常记录、患者及治疗普通回归 | `85925fb` | 782 通过，0 跳过；430.059 秒 |
| 全部 `postgres` 标记测试 | `85925fb` | 132 通过，0 跳过，2697 另选；454.098 秒 |
| 其中选定输出与部分作者清理竞争 | `85925fb` | 14 通过，0 跳过；58.563 秒 |
| 预览修复后的血糖及导出普通回归 | `1833105` | 370 通过，0 跳过；260.39 秒 |
| 原独审反例、生命周期和导出视图 | `1833105` | 16 通过，0 跳过；19.20 秒 |
| 新预览场景在 PostgreSQL 上执行 | `1833105` | 6 通过，0 跳过；24.672 秒 |
| 实际 Chromium 血糖、治疗及影像量化流程 | `1833105` | 12 通过，0 跳过；173.579 秒 |
| 独审原两条反例及六条永久预览场景 | `1833105` | 8 通过，0 跳过；29.70 秒 |
| 独审渲染后另一连接实际提交及撤权 | `1833105` | 7 通过，0 跳过；49.13 秒 |

范围有重叠，不能相加为唯一用例总数。132 项完整 PG 的结果保持原提交归属；后续新增
视图复检另有定向普通、PG 和浏览器验证，不冒称本地重跑了修复后的全部 PG；最终 CI 的
完整 PG 结果另列于下方。

随后文档契约及三个血糖评测工具模块共 144 通过、2 跳过（22.97 秒）；两项跳过是 Windows
创建符号链接权限不足的既有文档夹具，未跳过血糖评测工具用例。96 份文档、版本与发布自动化、
需求追踪、Django 状态及迁移检查通过，JavaScript 9 项通过。该阶段尚待运行的 PR CI，
现已按确切提交在下方补齐。

输出初审发现：HTML 已渲染后若记录更正或作者注销，视图仍返回旧卡片。原两条反例均失败，
永久 GET/非法格式 POST 的四个变化场景失败、两个无变化控制通过。修复在渲染后调用
真实 `get_preview`；失效清理提交后，返回不含旧快照、作者或生成表单的拒绝页。
独审进一步用不同连接在实际渲染后 COMMIT，更正、作者注销及成员撤权均不返回旧内容；
第三个连接直接读到已清空快照，没有通过再次调用预览服务制造清理。

更早的分阶段审查分别关闭并排报告串用时间、跨行借用时刻、换行未来计划限定、历史作者
清理、不可适配来源及作者注销死锁。最终选定输出初审的 PG 有 32 通过、3 项流关闭测试
夹具失败；用 `transaction=True` 私有包装调用原生产流后，三项全部通过。初始失败 XML、
原反例及后续结果均保留，没有修改应用来绕过检查。

浏览器实际验证 360px 手机与桌面录入、来源导入、跨季/显示时区、多值展开、真实 ZIP/PDF
下载及限时分享失效。此前 `85925fb` 的必跑组合为 20 通过、0 跳过，包含八项上传回归；
该工具自动删除临时 XML，记录采用当时实际工具控制台结果。上表新的 12 项保留独立 XML。
已查看集成阶段保存的合成页面截图，不将自动浏览器操作时间当作人工可用性研究。

## 两次固定原件评测

[首次结果](artifacts/batch-five-glucose-first-evaluation.json)、
[第二次结果](artifacts/batch-five-glucose-second-evaluation.json)及
[独立逐字段比较](artifacts/batch-five-glucose-comparison.json)均按原字节复制，保留各自源码
与制品身份。首次源码为 `b912815ae34c213b8c2d99125848922423a9b9f4`，第二次为
`2c7a67ab7785bfd8943b5a55c41a42f711ae6450`。后续输出和预览修复不改写这两次预测或评分。

两次都使用同一 64 文件、124 页及固定 OCR，真实流水线零失败，保留 761 个项目。
独立比较核对 128 份原件/OCR 身份、10 张已审原图和每次 54,741 个 OCR 块；旧预测使用
同一冻结评分器重新核对，未重跑预测、改 gold、补来源或改变分母。

固定正例只有 7 个已审来源行。首次恢复 3 行，第二次恢复 4 行；原 3 行及其原件证明均保留，
新增 1 行，仍漏 3 行。首次 43 个正确字段判断全部保留，新增 15 个正确判断，无旧正确项丢失；
这些是分别评分的字段，不能作为整报告或医学准确率。

| 字段类别 | 每项分母 | 首次 | 第二次 |
| --- | --- | --- | --- |
| 数值、原值、标本、采样/报告时间各分量、未知时区与不虚构 UTC，共 13 项 | 各 7 | 各 3 正确 / 4 缺失 | 各 4 正确 / 3 缺失 |
| 单位、原单位，共 2 项 | 各 7 | 各 2 正确 / 1 错配 / 4 缺失 | 各 2 正确 / 2 错配 / 3 缺失 |
| 来源种类 | 7 | 0 正确 / 3 来源未验证 / 4 缺失 | 0 正确 / 4 来源未验证 / 3 缺失 |
| 进餐时段及依据，共 2 项 | 各 7 | 各 0 正确 / 3 错配 / 4 缺失 | 各 1 正确 / 3 错配 / 3 缺失 |

`SOURCE_UNVERIFIED` 表示原件证明未满足冻结协议，不等于值已证实错误，也不能计为正确。
新增行仍带有两项单位错配和一项来源未验证，不能把“恢复一行”写成整行全对。
明确空腹层有 6 个目标，第二次仍为 0 正确、3 错配、3 缺失；唯一“未注明”目标已正确保留。
没有任何一行满足全部 18 项正确。

三张有明确排除标注的页面均无误接纳；14 个排除场景中，只有 2 个到达适配器并正确排除，
另 12 个为没有接纳，不能说全部 14 个都证明了排除规则。591 个范围外项目未被接纳，
100 页从未完成原图审阅的范围仍为未判断，不因执行成功变为阴性。

日期精度、缺单位、逐次护理表、印刷时区和 mg/dL 五类真实正例分母均为零，保留
`NOT_EVALUATED`；合成测试不能替代这些真实质量证据。第二次同一开发集重放的流水线
约 69.63 秒、比较约 6.53 秒，回放固定 OCR，不包括重新 OCR 或图像预处理时间，也不是独立留出集。

## 源码交付与最终 CI

功能头 `8171822f8b0ca615fc5ef62377966c7a4d5b1d94` 经最终增量独审后，Squash 为
`830f06b2dd1d745fa79e0c92cefe9b13527e769b`。Release Please 在
[发布 PR #64](https://github.com/skuyd/emr/pull/64) 确定 `1.13.0`，检查通过后自动合并为
`92825c287e35f72bc3df0420eee72bb1e0d2f76a`；标签指向该提交，GitHub Release 于
`2026-09-08T07:04:42Z` 发布。

| 确切 CI | Python 普通回归 | PostgreSQL | 必跑上传浏览器 | JavaScript |
| --- | --- | --- | --- | --- |
| [功能头 8171822](https://github.com/skuyd/emr/actions/runs/34194673245) | 2684 通过、4 跳过、139 未选入；674.37 秒 | 132 通过、0 跳过；163.63 秒 | 8 通过、0 跳过；27.24 秒 | 9 通过、0 跳过 |
| [发布头 562086b](https://github.com/skuyd/emr/actions/runs/34196428860) | 2684 通过、4 跳过、139 未选入；720.60 秒 | 132 通过、0 跳过；149.31 秒 | 8 通过、0 跳过；26.58 秒 | 9 通过、0 跳过 |

两套 CI 的标题、容器构建、普通测试和 PostgreSQL 四项检查全部成功。Ubuntu 的四项跳过
来自未修改的 Windows PowerShell 启动器测试，不属于必跑浏览器；PG 每套另有 2703 项未选入。
这些测试范围有重叠，不相加为唯一测试总数。

844 份应用、配置、静态、模板、测试和工具的 Git 文件身份，在 `1833105`、最终功能头、
功能 Squash、发布头及发布标签间完全相同。自动发布仅改六个版本/Changelog 文件，未修改
应用逻辑。Git blob 与 Windows checkout 换行分别记录，不将 LF/CRLF 差异称为原字节一致。

新的[交付制品](artifacts/batch-five-glucose-delivery.json)绑定实际 API、CI 日志、独审和源码
身份。此前四份公开制品保留原字节与原阶段，其中测试制品的待发布状态是历史记录；当前
交付状态以登记表和本次交付制品为准。本次回填没有重跑 OCR、真实预测或改变原评分。

## 交付边界

B5-01 日常记录已有
[独立交付证据](batch-five-daily-records.md)；剩余 B3 临床结构化及五批整体验收继续实施。
既有需求追踪保持 60 项已验证、2 项待外部验证；
[生产门禁](release-gate.md)仍为 `BLOCKED`（8 项通过、15 项待验证），本任务未部署生产。
